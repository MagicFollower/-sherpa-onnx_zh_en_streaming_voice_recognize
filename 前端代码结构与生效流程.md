# 听间（LASR）前端代码结构、功能分布与生效流程

- 文档修订日期：2026-09-22。代码分析基于项目当前源码实际内容，不涉及运行验证。
- 主要读者：具备 Vue 3 与 TypeScript 基础、需要理解本前端项目内部机制的开发者。读完本文后，能定位任意功能点的代码归属，理解一次录音从用户按住按钮到文字出现在页面上的完整数据流，并在出现异常时沿分析链路缩小原因。
- 文类：概念说明（模块机制与数据流）+ 参考（接口与状态定义）+ 排障（第 10 章问题记录）。
- 适用范围：前端 `src/` 目录下全部 TypeScript 与 Vue 源码，以及构建配置与测试基础设施。后端代码仅在接口边界处提及，不作为本文主体。

---

## 1 项目定位与技术选型

### 1.1 项目是什么

听间（LASR Local Voice Studio）是一个**本地离线实时语音转写**系统的前端界面。用户在浏览器中按住按钮说话，音频通过 WebSocket 流式传输到同一局域网内运行的后端服务，后端使用离线模型完成识别后将文字逐步返回。前端职责是：采集麦克风音频、管理配对与会话、驱动录音轮次生命周期、展示实时草稿与终稿。

**前端不承担的职能**：模型加载与推理、音频解码为文字、会话持久化、跨设备同步。这些由后端服务完成。

### 1.2 核心技术选型

| 技术 | 版本 | 职责 | 为什么选择 |
|---|---|---|---|
| Vue 3 | 3.5.43 | 响应式 UI 框架 | Composition API 适合将音频/协议/轮次逻辑拆分为 composable |
| TypeScript | 5.9.3 | 类型安全 | 严格的协议校验需要精确类型；`noUncheckedIndexedAccess` 开启 |
| Vite | 8.3.0 | 构建与开发服务器 | 原生 ESM 开发；worker 格式配置支持 AudioWorklet 打包 |
| Vitest | 5.0.1 | 单元测试 | 与 Vite 共享配置，jsdom 环境 |
| Web Audio API | 浏览器原生 | 音频采集与 Worklet 线程 | AudioWorklet 提供独立音频线程，避免主线程卡顿影响采集 |
| WebSocket | 浏览器原生 | 实时双向通信 | 音频帧流式传输需要低延迟双向通道 |

**不使用的技术**：无 Vuex/Pinia（状态由 composable + reactive 管理）；无 axios（直接使用 fetch）；无 Service Worker；无远程字体或 CDN 资源。

### 1.3 运行环境约束

- **可信上下文（Secure Context）**：浏览器要求 `getUserMedia` 和 `AudioWorklet` 只能在 HTTPS 或 `localhost` 下使用。局域网非 localhost 访问必须配置 HTTPS。
- **AudioWorklet 支持**：浏览器必须支持 `AudioWorkletNode`，用于在独立线程执行 FIR 重采样和 PCM 量化，主线程阻塞不会影响音频采集。
- **getOutputTimestamp**：AudioContext 的 `getOutputTimestamp()` 方法提供音频时钟到 performance 时钟的映射，是精确确定松手采样边界的关键。

---

## 2 目录结构与模块职责分布

### 2.1 文件清单与职责

```
前端/
├── index.html                  # 入口 HTML；lang="zh-CN"，无外部资源引用
├── package.json                # 依赖声明；仅 vue 为运行时依赖
├── vite.config.ts              # 构建配置与开发代理
├── vitest.config.ts            # 测试配置
├── tsconfig.json               # TypeScript 编译选项
├── src/
│   ├── main.ts                 # 应用入口：createApp(App).mount('#app')
│   ├── App.vue                 # 根组件：布局骨架、状态绑定、composable 消费
│   ├── style.css               # 全局样式（CSS 变量、响应式断点）
│   ├── protocol/               # 协议层：消息类型定义、运行时校验、音频编码
│   │   ├── messages.ts         # 所有消息类型与常量的唯一定义
│   │   ├── schema.ts           # 运行时校验函数（parseObject、parseServer 等）
│   │   └── audio.ts            # PCM 量化、二进制帧编码、AudioLedger 帧账本
│   ├── audio/                  # 音频采集链路：从麦克风到 16kHz PCM 帧
│   │   ├── capture.ts          # BrowserCapture：getUserMedia 与 Worklet 生命周期
│   │   ├── capture.worklet.ts  # AudioWorklet 处理器（音频线程专用）
│   │   ├── clock.ts            # AudioClock：音频时钟与 performance 时钟的桥梁
│   │   ├── fir.ts              # StreamingFIR：流式 FIR 重采样（任意采样率→16kHz）
│   │   └── pipeline.ts         # CapturePipeline：100ms 提交窗、帧打包、排空
│   ├── services/               # 网络服务层：HTTP API 与 WebSocket 连接
│   │   ├── api.ts              # LocalApi：pair/logout/status HTTP 调用
│   │   └── connection.ts       # LasrConnection：WebSocket 会话、心跳、协议校验
│   ├── state/                  # 状态管理：轮次生命周期与输入所有权
│   │   ├── rounds.ts           # RoundManager：录音轮次的创建、消息处理、终态
│   │   └── input.ts            # HoldInput：指针/键盘输入所有权与事件绑定
│   ├── composables/            # Vue Composable
│   │   └── useStudio.ts        # 顶层 composable：组装 API、连接、轮次、视图
│   └── components/             # Vue 组件
│       ├── PairCard.vue        # 配对表单
│       ├── TranscriptList.vue  # 转写卡片列表
│       ├── HoldControls.vue    # 底部操作栏（按住按钮、取消、复制、清空）
│       └── StudioIcon.vue      # SVG 图标组件
└── tests/
    ├── setup.ts                # 测试全局设置（TextEncoder polyfill、PointerEvent polyfill）
    ├── fixtures.ts             # 测试数据工厂（ready/ack/final 消息构造）
    └── unit/
        └── protocol.test.ts    # 协议层单元测试
```

### 2.2 模块依赖关系

模块依赖严格单向，从底层协议到顶层组件形成清晰的层次：

```
components/ ──→ composables/useStudio ──→ services/api
      │                  │                services/connection
      │                  │                     │
      │                  ├──→ state/rounds ──→ protocol/messages
      │                  │        │            protocol/schema
      │                  │        ├──→ protocol/audio
      │                  │        └──→ audio/capture
      │                  │                  ├── audio/clock
      │                  │                  ├── audio/pipeline ──→ audio/fir
      │                  │                  └── audio/capture.worklet
      │                  └──→ state/input
      └──→ components/*（子组件引用）
```

**关键约束**：
- `protocol/` 不依赖任何其他模块，是最底层。
- `audio/` 只依赖 `protocol/`（消息类型和音频常量）。
- `services/` 只依赖 `protocol/`（消息解析和校验）。
- `state/` 依赖 `protocol/` 和 `audio/`（音频采集接口）以及 `services/`（Transport 接口）。
- `composables/` 组装所有模块，是唯一的集成点。
- `components/` 只消费 composable 暴露的响应式状态和函数。

### 2.3 运行时依赖与开发依赖分离

`package.json` 中唯一运行时依赖是 `vue`。所有其他包（Vite、Vitest、TypeScript、Playwright 等）均为开发依赖，不进入生产构建产物。生产构建由 Vite 输出到 `dist/`，由后端同源静态文件服务提供。

---

## 3 协议层：消息定义、运行时校验与音频编码

协议层是整个前端最底层的模块，定义了前后端通信的所有数据结构与校验规则。任何从网络进入前端的数据都必须先经过协议层的运行时校验，TypeScript 类型断言不替代运行时检查。

### 3.1 messages.ts：唯一字段定义

文件 [messages.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/protocol/messages.ts) 是 LASR 1.0 协议所有常量和类型的唯一定义位置。

**核心常量**：

| 常量 | 值 | 含义 |
|---|---|---|
| `VERSION` | `'1.0'` | 协议版本标识 |
| `AUDIO` | `{ sample_rate: 16000, channels: 1, format: 'pcm_s16le', frame_samples: 320 }` | 音频参数。16kHz 单声道 16-bit PCM，每帧 320 采样（20ms） |
| `UUID` | 正则表达式 | UUID v4 格式校验 |

**消息类型体系**：

所有服务端到客户端的消息共享 `Base` 接口（`type` + `protocol_version` + `session_id`），按职责分为：

- **会话级**：`Hello`（连接问候，携带 `model_ready`、`config_id`、`max_active`、`max_utterance_ms`）、`Heartbeat`（ping/pong 保活）。
- **轮次级**：`Ready`（服务端确认开始，携带音频参数和功能开关）、`Ack`（音频帧接收确认）、`Partial`（实时草稿）、`Final`（终稿）、`Cancelled`（取消确认）、`ServerError`（错误）。
- **HTTP 级**：`Status`（`/api/status` 响应体，携带 `model_ready`、`busy`、`connection_available`）。

**类型设计原则**：

`Ready` 消息中 `punctuation_enabled`、`refinement_enabled`、`itn_enabled` 全部字面量类型 `false`，`Final` 中 `result_mode` 限定为 `'streaming'`，`degraded` 限定为 `false`。这不是"未来可能变化"的预留字段，而是当前版本明确关闭全部增强功能的声明。校验层会强制检查这些值，服务端如果悄悄改变模式会被前端拒绝。

### 3.2 schema.ts：运行时校验函数

文件 [schema.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/protocol/schema.ts) 提供所有从网络接收数据的运行时校验。TypeScript 类型只在编译期存在，运行时 JSON 数据可以包含任意字段，因此每次解析后必须经过显式校验。

**基础校验工具**：

| 函数 | 职责 |
|---|---|
| `bytes(s)` | 计算字符串 UTF-8 字节长度（使用 TextEncoder） |
| `requireThat(ok)` | 断言函数；失败抛出 `ProtocolFault` |
| `integer(v, min, max)` | 安全整数范围检查 |
| `parseObject(raw)` | JSON 解析 + UTF-8 字节上限（16384）+ 对象深度限制（≤4 层）+ 非数组检查 |

**`parseObject` 的防御设计**：

先检查 UTF-8 字节长度再解析 JSON，避免超大 payload 消耗解析资源。解析后递归检查对象深度不超过 4 层，防止深层嵌套的攻击性 payload。这个函数是所有网络输入的统一入口，HTTP 响应和 WebSocket 消息都经过它。

**`parseServer` 消息分派校验**：

`parseServer(raw)` 先调用 `parseObject` 和 `common`（版本 + session_id + UUID 校验），再按 `type` 字段分派到对应校验分支：

- `hello`：校验 `config_id`、`max_active === 1`、`max_utterance_ms === 60000`、`model_ready` 布尔值。
- `ready`：校验 `config_id`、音频参数与 `AUDIO` 常量逐字段匹配、功能开关全部 `false`。
- `ack`：校验接收/处理的序列号和采样数相互一致（`processed ≤ received`），`stop_received` 的单调性。
- `partial`：校验 `revision ≥ 1`、`text` 字节长度 ≤ 8192。
- `final`：校验 `revision` 单调递增、`status` 与 `text` 的互斥关系（`empty` 当且仅当空文本）、音频计数与 `AudioLedger` 的期望值匹配、增强功能全部关闭。
- `cancelled`：校验 `reason` 在允许的取消原因枚举中。
- `error`：校验 `code`/`message` 长度、`scope`/`terminal`/`retryable`/`action` 的组合约束（如 `terminal` 只能在 `utterance` scope 中为 true）。

任何校验失败都抛出 `ProtocolFault`，上层统一捕获后关闭连接。前端不尝试修复协议不一致，而是立即断开并要求用户重连。

### 3.3 audio.ts：PCM 量化、二进制帧编码与帧账本

文件 [audio.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/protocol/audio.ts) 负责音频数据的编码与计数。

**`quantize(value)` — 浮点到 Int16 量化**：

将 [-1, 1] 范围的浮点采样转换为 16-bit 有符号整数。采用 `Math.trunc`（向零截断）而非 `Math.round`，因为负满幅（-32768）与正满幅（32767）不对称，`Math.round` 会在负端引入偏移。

**`encodeAudio(id, seq, samples)` — 二进制帧编码**：

将音频数据打包为 LASR 二进制帧格式：

```
偏移  长度  内容
0     4     魔数 "LASR"（0x4C 0x41 0x53 0x52）
4     1     版本（1）
5     1     保留
6     2     头长度（32，小端）
8     16    UUID（逐字节大端，不用 Windows GUID mixed-endian）
24    4     帧序号 seq（小端 uint32）
28    2     采样数（小端 uint16）
32    N×2   PCM 采样（Int16，小端）
```

UUID 编码特别注明不使用 Windows GUID 的 mixed-endian 字段布局。标准 UUID 文本表示 `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx` 去除连字符后逐字节解析，确保跨平台一致。

**`AudioLedger` — 帧账本**：

`AudioLedger` 维护已发送音频帧的累计采样水位（每个帧的末尾采样位置），用于验证服务端 ACK 的正确性。

核心状态：
- `ends[]`：每帧末尾的累计采样数（最多 3000 帧，对应 60 秒上限）。
- `received` / `processed`：服务端确认的接收和处理采样数。
- `stopped` / `stopReceived`：本地是否已发送 stop、服务端是否已确认收到 stop。

`accept(ack)` 方法校验 ACK 中的序列号和采样数与本地账本完全匹配：`ends[received_seq] === received_samples`，且 `processed ≤ received`。如果 `stop_received` 为 true，要求本地已 stop 且 received 恰好等于最后一帧。

`final(final)` 方法校验终稿消息中的 `last_seq` 和 `total_samples` 与账本一致，且所有帧已被服务端确认处理完毕。

这种"先记账后对账"的模式确保音频帧不会丢失、重复或乱序，任何不一致都触发 `ProtocolFault`。

---

## 4 音频采集链路：从麦克风到网络帧

音频采集链路运行在两个线程上：主线程负责设备管理和时钟同步，AudioWorklet 线程负责实时音频处理（FIR 重采样、PCM 量化、帧打包）。这个分离设计确保主线程的 JavaScript 执行（如 Vue 响应式更新）不会导致音频采集丢帧。

### 4.1 capture.ts — BrowserCapture：设备生命周期

文件 [capture.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/audio/capture.ts) 中的 `BrowserCapture` 类是音频采集的生产入口，实现 `AudioCapture` 接口。

**`prepare()` — 设备准备阶段**：

1. **能力检查**：调用 `audioCapability()` 检查 Secure Context、`getUserMedia`、`AudioContext` 和 `AudioWorkletNode` 是否可用。
2. **AudioContext 创建**：使用 `latencyHint: 'interactive'` 创建上下文，并在用户手势同步阶段立即调用 `resume()`，避免移动浏览器在权限 await 之后拒绝自动播放。
3. **麦克风获取**：`getUserMedia` 请求单声道音频，关闭回声消除、自动增益和噪声抑制（这些处理由后端模型负责）。
4. **dispose 检查**：在 `getUserMedia` 返回后和 `resume` 完成后各检查一次 `disposed` 标志。如果用户在权限等待期间取消操作，已获取的麦克风流会被立即释放。
5. **Worklet 加载**：通过 Vite 的 `?worker&url` 导入方式加载 `capture.worklet.ts`，注册为 `lasr-capture` 处理器。
6. **节点连接**：`MediaStreamSource → AudioWorkletNode → context.destination`。连接 destination 不是为了播放音频（Worklet 输出始终为零），而是让渲染时钟持续运行，使 `getOutputTimestamp()` 持续提供有效的时间戳。
7. **AudioClock 稳定**：以 20ms 间隔轮询 `getOutputTimestamp()`，等待 AudioClock 确认音频时钟与 performance 时钟的映射关系稳定（至少 5 秒的 elapsed 验证）。1.5 秒超时。
8. **时钟观察定时器**：每 40ms 调用一次 `observe()`，持续更新 AudioClock 的时间戳映射。

**`stop(eventTime)` — 松手停止阶段**：

1. 最后一次调用 `observe()` 刷新时钟映射。
2. 通过 `AudioClock.frameAt()` 将松手的 `eventTime`（PointerEvent 或 KeyboardEvent 的时间戳）转换为音频帧位置。
3. 立即调用 `stopTracks()` 关闭麦克风（不等 final 消息，麦克风尽早释放）。
4. 向 Worklet 发送 `stop` 消息和截止帧位置。
5. Worklet 裁剪已缓存源样本到截止帧，drain 所有待输出样本，返回 `drained` 事件。

**`dispose()` — 资源清理**：

标记 `disposed`，停止麦克风流，清除时钟定时器，向 Worklet 发送 `discard` 消息，断开所有节点连接，关闭 AudioContext。reject 所有待解决的 drain Promise。dispose 是幂等的，重复调用不报错。

### 4.2 capture.worklet.ts — AudioWorklet 处理器

文件 [capture.worklet.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/audio/capture.worklet.ts) 运行在 AudioWorklet 的独立线程中。它不引用任何 DOM API 或应用状态，只通过 `MessagePort` 与主线程通信。

**LasrCapture 类**继承 `AudioWorkletProcessor`：

- **消息处理**：接收 `start`（创建 `CapturePipeline`）、`stop`（调用 pipeline.stop）、`processed`（通知已确认采样数）、`discard`（丢弃并标记失败）。
- **process() 回调**：浏览器音频线程以 128 采样为一个 render quantum 调用。将输入传入 `pipeline.push()`，附带 `currentFrame`（AudioWorklet 全局变量，绝对帧位置）。
- **输出为零**：`process()` 不写入输出缓冲区，麦克风音频永远不会回放到扬声器。连接 destination 只是为了让渲染时钟工作。
- **Transferable 优化**：PCM 事件使用 `postMessage(event, [event.samples.buffer])` 转移 ArrayBuffer 所有权，避免跨线程拷贝。

### 4.3 clock.ts — AudioClock：双时钟桥梁

文件 [clock.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/audio/clock.ts) 中的 `AudioClock` 解决一个核心问题：用户松手事件的时间戳来自 `performance.now()` 时钟，而音频采样的位置由 AudioContext 的 `currentTime`（等价于 `currentFrame / sampleRate`）确定。两个时钟独立运行，需要显式建立映射。

**`observe(next, now)` — 时钟映射更新**：

接收 `getOutputTimestamp()` 返回的 `{ contextTime, performanceTime }` 和当前的 `performance.now()`。校验条件：
- 两个时间戳均为有限正数。
- `performanceTime` 与 `now` 的差距不超过 100ms（防止过旧时间戳）。
- 如果已有上一次记录，两次之间的 performance 时钟和 audio 时钟的 elapsed 差异不超过 3ms（检测时钟跳变或暂停）。
- elapsed ≥ 5 秒后标记为 `stable`。

**`frameAt(eventTime, now, timeOrigin)` — 事件时间到音频帧的转换**：

将 PointerEvent/KeyboardEvent 的 `eventTime` 转换为 AudioContext 坐标系中的帧位置。计算公式：

```
frame = floor((stamp.contextTime + (eventTime - stamp.performanceTime) / 1000) × sampleRate)
```

其中 `stamp` 是最后一次有效的 `observe` 记录。校验事件时间不能在未来 2ms 以上或过去 100ms 以上。

`eventTime > 1e12` 时判断为 Unix 时间戳格式（如 `Date.now()`），需减去 `performance.timeOrigin` 转为 performance 相对时间。

### 4.4 fir.ts — StreamingFIR：流式重采样

文件 [fir.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/audio/fir.ts) 实现任意输入采样率到 16kHz 的流式 FIR（有限脉冲响应）重采样。

**为什么需要重采样**：浏览器 AudioContext 的采样率取决于硬件（常见 44100Hz 或 48000Hz），而后端模型固定要求 16kHz 输入。FIR 重采样比简单的线性插值或抽点能提供更好的抗混叠性能。

**设计要点**：

- **中心对称窗化 sinc**：半径 64 点，使用 Blackman-Harris 窗函数（0.42 + 0.5cos + 0.08cos2π），截止频率为 0.45 × min(1, 16000/sourceRate)。
- **多相位预计算**：将 sinc 核按相位拆分为 `16000 / gcd(sourceRate, 16000)` 组，避免逐点计算三角函数。例如 44100Hz 输入产生 147 个相位（16000/gcd(44100,16000) = 16000/100 = 160... 实际由 gcd 决定）。
- **环形缓冲区**：4096 采样固定大小，`sourceTotal % 4096` 索引。
- **累计整数计数**：输出位置由 `floor(sourceTotal × 16000 / sourceRate)` 决定，不使用逐块浮点累加，避免 44.1kHz 长轮产生漂移。
- **跨块延迟**：在线模式下，每个源采样进入后等待右侧 64 点可用才输出（`center + radius < sourceTotal` 时才 produce）。排空时用零填充。
- **不延长录音**：排空只处理已缓存的源样本，不额外请求新数据。

### 4.5 pipeline.ts — CapturePipeline：提交窗与帧打包

文件 [pipeline.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/audio/pipeline.ts) 是 AudioWorklet 线程中的核心处理管线，连接原始音频输入和 FIR 重采样输出。

**100ms 提交窗**：

FIR 滤波器需要"未来"的采样点来正确计算当前输出（右侧 64 点的延迟）。Pipeline 保留最近 100ms（`ceil(rate × 0.1)` 采样）的源数据不送入 FIR，作为延迟缓冲。这意味着用户松手后，最后 100ms 的音频仍然会被正确处理（从缓冲中取出并经过 FIR），不会丢失。

**`push(channels, absoluteFrame)` — 音频块输入**：

1. 以 `absoluteFrame`（AudioWorklet 的 `currentFrame`）检查连续性；不连续则抛出 `AUDIO_CLOCK_DISCONTINUITY`，不补零掩盖。
2. 多声道混合为单声道（求均值）。
3. 累计采样不超过 `rate × 60`（60 秒上限）。
4. 将源数据写入环形 `pending` 缓冲区。
5. 计算可提交位置 `sourceTotal - ceil(rate × 0.1)`，送入 FIR。
6. FIR 输出的 16kHz 采样经过量化（Int16）后累积到 320 采样帧（20ms），满帧发送 `pcm` 事件。
7. 检查积压：`produced - processed ≥ 32000`（2 秒未确认）抛出 `CLIENT_OVERLOADED`。

**`stop(cutoffFrame)` — 截止与排空**：

将 `sourceTotal` 裁剪到 `cutoffFrame - origin`，提交所有剩余缓冲到 FIR，调用 `fir.finish()` 排空延迟线，发送最后一个不满 320 采样的短帧，发出 `drained` 事件。

如果 `cutoffFrame` 早于已提交到 FIR 的位置（`target < fed`），抛出 `AUDIO_CUTOFF_UNCERTAIN`——已经进入 FIR 的数据无法撤回，不能偷偷截短。

---

## 5 服务层：HTTP API 与 WebSocket 连接

服务层封装前后端之间的网络通信，向上提供 `pair`、`logout`、`status` 三个 HTTP 操作和 `LasrConnection` 一个 WebSocket 会话。

### 5.1 api.ts — LocalApi：HTTP 客户端

文件 [api.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/services/api.ts) 实现 HTTP API 客户端。

**构造函数与 fetch 绑定**：

```typescript
constructor(private request: typeof fetch = (...args: Parameters<typeof fetch>) => fetch(...args)) {}
```

默认使用箭头函数包装 `fetch`，保持全局调用上下文。直接写 `fetch` 作为默认参数会导致 `this` 丢失（详见第 10 章问题 3）。构造函数接受注入 `fetch` 替代物，供测试使用。

**`call(path, body?)` — 统一请求方法**：

1. 创建 `AbortController`，5 秒超时自动 abort。
2. `credentials: 'same-origin'` 确保 cookie 随请求发送，但不跨域。
3. `cache: 'no-store'` 禁用浏览器缓存。
4. 响应处理：
   - 检查 UTF-8 字节长度 ≤ 16384。
   - 非 2xx 状态码：解析 JSON 错误体，校验 `code` 和 `message` 字段，抛出 `ApiError`。
   - POST 请求（有 body）：期望 204 空响应。
   - GET 请求：期望 200 + `application/json` Content-Type。
   - 任何不符合预期的响应格式都抛出 `ProtocolFault`。

**`ApiError` 与用户友好消息**：

HTTP 状态码到中文提示的映射在 `messages` 对象中定义。`ApiError` 携带 `status`、`code`、`message` 和 `retryAfter`（仅 429 状态码时有效），上层可以区分"需要重试"和"需要用户操作"。

### 5.2 connection.ts — LasrConnection：WebSocket 会话

文件 [connection.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/services/connection.ts) 管理 WebSocket 连接的生命周期。

**`socketUrl(location)` — URL 构造**：

- HTTPS → `wss://host/ws`
- HTTP + localhost/127.0.0.1/[::1] → `ws://host/ws`
- 其他 HTTP → 抛出错误（局域网必须 HTTPS）

**构造阶段**：

接受 `ConnectionEvents`（`message` 和 `closed` 回调）和可选的 WebSocket 工厂函数（测试注入用）。立即建立 WebSocket 连接，设置 5 秒 hello 超时。

**消息处理流程**：

1. 首条消息必须是 `hello` 类型，且 WebSocket 子协议为 `lasr.v1`。提取 `session_id`，清除 hello 超时，启动 1 秒心跳定时器。
2. 后续消息不允许是 `hello`，且 `session_id` 必须匹配。
3. `heartbeat` 类型中 `ping` 自动回复 `pong`；不转发给上层。
4. 其他消息转发给 `events.message` 回调。
5. 任何解析或校验异常触发 `fail()`，关闭连接并通知上层。

**心跳机制**：

每秒检查一次：
- 30 秒内没有收到任何有效消息 → 心跳超时，关闭连接。
- 10 秒内没有发送过 ping → 发送一个携带 UUID nonce 的 ping。

**`send(fields)` — 消息发送**：

JSON 消息自动注入 `protocol_version` 和 `session_id`，并在发送前经过 `parseObject` 校验（防止前端代码生成非法消息）。二进制消息（ArrayBuffer）直接发送。

**`close()` — 正常关闭**：

设置 `done` 标志，清除所有定时器和事件处理器，发送 1000 正常关闭码。`close()` 是幂等的。

**`fail(reason)` — 异常关闭**：

如果尚未关闭，调用 `close()` 并触发 `events.closed`。`fail` 确保不会重复通知。

---

## 6 状态管理：轮次生命周期与输入所有权

状态层是前端最复杂的模块，管理录音轮次从创建到终态的完整生命周期，以及用户输入（指针/键盘）的所有权。

### 6.1 rounds.ts — RoundManager：轮次生命周期

文件 [rounds.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/state/rounds.ts) 中的 `RoundManager` 是整个录音流程的核心控制器。

**核心概念**：

- **Card（卡片）**：一次录音轮次的视图表示，包含 `id`（UUID）、`phase`（阶段）、`text`（文字内容）、`revision`（版本号）、`samples`（采样数）等。
- **Active（活动槽）**：当前正在进行的轮次的内部状态，包含 Card、AudioCapture 实例、AudioLedger、generation 计数器等。任何时刻最多一个 Active。
- **Generation（代际）**：递增整数，作为异步操作的撤销栅栏。当用户取消或新轮次开始时 generation 递增，旧的异步回调通过检查 generation 来判断自身是否仍然有效。
- **Phase（阶段）**：`preparing → recording → draining → finalizing → completed` 是正常路径；任意阶段可进入 `cancelling → cancelled` 或 `failed`。

**`begin(source)` — 轮次创建**：

1. 前置检查：无活动轮次、有有效 transport session、`canStart` 条件满足。
2. 创建 Card 并推入 `view.cards`（超过 50 张时移除最旧的）。
3. 创建 Active 对象，包含新的 `AudioCapture` 实例和 `AudioLedger`。
4. Vue 响应式代理可能使 `view.cards.at(-1)` 返回的对象与原 `card` 不同，后续所有写入都使用视图中的引用。
5. 异步调用 `prepare()`。

**`prepare(a, source)` — 异步准备**：

1. 调用 `capture.prepare()` 获取采样率。
2. generation 检查：如果已被取消则释放 capture 并返回。
3. 发送 `start` 消息给后端，设置 5 秒准备超时。
4. 异常时调用 `finish(a, 'failed', ...)`。

**`release(eventTime)` — 松手处理**：

1. 标记 `held = false`。
2. 如果仍在 preparing 阶段，直接取消（用户松手太快）。
3. 如果不在 recording 阶段，忽略。
4. 切换到 draining 阶段，设置 15 秒总超时和 2 秒 drain 超时。
5. 调用 `capture.stop(eventTime)`，等待 drain 完成。
6. drain 完成后调用 `stopped()`。

**`stopped(a, result)` — 采集结束**：

1. 校验 `result.total === ledger.total`，确保 Pipeline 和 Ledger 的采样计数一致。
2. 调用 `ledger.stop()` 标记不再发送。
3. 发送 `stop` 消息给后端。
4. 切换到 finalizing 阶段，设置 15 秒等待终稿超时。

**`message(msg)` — 服务端消息处理**：

按消息类型分派：

| 消息类型 | 处理 |
|---|---|
| `ready` | 清除准备超时；切换到 recording 阶段；启动 capture 的回调（pcm/progress/fault/limit）；设置 65 秒绝对超时 |
| `ack` | 调用 `ledger.accept()` 校验计数；通知 capture 已确认采样数；更新积压警告 |
| `partial` | 校验 revision 单调递增；更新卡片文字 |
| `final` | 调用 `ledger.final()` 校验计数；更新终稿文字；切换到 completed |
| `cancelled` | 校验 reason 匹配；切换到 cancelled 终态 |
| `error` | terminal 错误或 preparing 阶段的 BUSY/MODEL_NOT_READY → 切换到 failed |

**`cancel(reason, note)` — 取消处理**：

1. 递增 generation（使旧异步回调失效）。
2. 标记 `discarded = true`，清空文字。
3. 释放 capture，清除定时器。
4. 如果尚未发送 start 消息，直接进入 cancelled 终态。
5. 否则发送 cancel 消息给后端，设置 2 秒确认超时，切换到 cancelling 阶段。
6. **关键**：立即清理 `this.active` 和 `view.activeId`，不等后端 cancelled 确认。generation 已递增，后续收到的 cancelled 消息会通过 `finish()` 中的 `this.active !== a` 检查被忽略。

**`finish(a, phase, note)` — 终态写入**：

`RoundManager` 是页面终态的唯一写入者。只有当 `this.active === a` 时才执行：释放 capture、清除定时器、递增 generation、设置终态阶段和说明、清除 `activeId`。

### 6.2 input.ts — HoldInput：输入所有权

文件 [input.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/state/input.ts) 中的 `HoldInput` 类管理"按住讲话"的输入所有权，解决一个关键问题：**60 秒自动结束后，用户手指仍然按在按钮上，不能自动续录**。

**所有权模型**：

`Owner` 是一个联合类型 `{ source: 'pointer', id: number } | { source: 'keyboard' }`。只有当前拥有者才能触发 release。这确保：
- 非 primary 指针不触发。
- 鼠标右键不触发。
- 键盘 repeat 事件不重复触发 begin。
- IME 输入法组合状态不触发。

**指针事件处理**：

1. `pointerdown`：检查 primary、左键、`begin()` 返回 true → 获取 pointer capture。如果 capture 获取失败，立即 abort。
2. `pointerup`：验证拥有者身份 → 清除 owner → 调用 `release(eventTime)` → 释放 pointer capture。顺序很重要：先清 owner 再释放 capture，随后的 `lostpointercapture` 事件不会把合法的 drain 改成取消。
3. `pointercancel` / `lostpointercapture`：只有当前拥有者才触发 abort。

**键盘事件处理**：

空格键（`Space`）作为按住讲话键。排除条件：repeat 事件、IME 组合状态、当前有拥有者、焦点在可编辑控件上。Escape 键无条件取消当前轮。

**`abort(reason)` — 强制取消**：

清除 owner，调用 `cancel(reason)`，如果是指针拥有者则释放 capture。由 `blur`（窗口失焦）、`visibilitychange`（页面隐藏）、`pagehide`（页面卸载）触发。

**`attach()` / `dispose()` — 事件绑定生命周期**：

`attach()` 将所有事件处理器绑定到按钮和 window/document，并保存解绑函数。`dispose()` 先 abort 再解绑所有事件。

### 6.3 useStudio.ts — 顶层 Composable

文件 [useStudio.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/composables/useStudio.ts) 是整个前端的组装点，将 API、连接、轮次和视图状态组合在一起。

**响应式状态**：

| 状态 | 类型 | 含义 |
|---|---|---|
| `view` | `reactive(RoundView)` | 卡片列表、活动卡片 ID、屏幕阅读器公告、积压警告 |
| `connection` | `reactive({...})` | 配对、连接、模型就绪、忙碌、错误等状态 |
| `notice` | `ref('')` | 全局提示消息 |
| `capability` | `ref<string>()` | 浏览器能力缺失提示 |
| `clock` | `ref(Date.now())` | 每秒更新，驱动重试倒计时 |

**计算属性**：

- `canStart`：所有开始条件满足（已连接、模型就绪、不忙碌、无活动轮次、无能力缺失）。
- `active`：当前活动卡片。
- `latest`：最近一张已完成且有文字且未丢弃的卡片。
- `retrySeconds`：429 重试倒计时。

**`refresh(connect)` — 状态刷新**：

调用 `/api/status` 获取后端状态。如果 `connect` 为 true 且尚无 WebSocket 连接，检查 `connection_available` 后创建 `LasrConnection`。连接关闭时自动调用 `refresh(false)` 重新轮询状态。

**`pair(code)` — 配对流程**：

调用 `api.pair(code)`，成功后调用 `refresh(true)` 建立 WebSocket 连接。generation 栅栏确保组件卸载后的回调不生效。

**`logout()` — 注销流程**：

递增 generation → 取消当前轮次 → 关闭 WebSocket → 调用 `api.logout()` → 重置所有连接状态。

**生命周期**：

- `onMounted`：检查浏览器能力 → 立即 `refresh(true)` → 启动 1 秒时钟和 10 秒状态轮询。
- `onUnmounted`：标记 disposed → 递增 generation → 清除定时器 → 关闭连接 → 销毁 RoundManager。

---

## 7 组件层：界面结构与交互职责

组件层是纯粹的视图和交互层，不直接操作网络或音频。所有数据通过 props 流入，所有操作通过 emit 流出。

### 7.1 App.vue — 根组件与布局骨架

文件 [App.vue](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/App.vue) 定义页面整体结构：

- **顶部导航栏**：品牌标识、连接状态药丸（等待配对/连接中/本地已连接/连接已断开）、注销按钮、帮助链接。
- **介绍区**：标题"让表达，自然流动"和产品描述。
- **工作区网格**：左侧边栏（记录计数、模型状态）+ 右侧内容区（错误提示、配对卡片、转写列表）。
- **帮助面板**：折叠式说明（按住操作、连接方式、隐私说明、麦克风问题）。
- **底部操作栏**：通过 `HoldControls` 组件渲染。
- **辅助功能**：跳转链接（skip-link）、屏幕阅读器实时公告区（`aria-live="polite"`）、toast 提示。

`App.vue` 调用 `useStudio()` 获取所有状态和函数，通过 props 和 emit 传递给子组件。`actions` 对象将 `RoundManager` 的方法适配为 `HoldInput` 所需的接口。

### 7.2 PairCard.vue — 配对表单

文件 [PairCard.vue](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/components/PairCard.vue) 渲染配对输入表单。

- 输入框类型 `password`，关闭自动完成和拼写检查，大写锁定自动转换（`autocapitalize="characters"`）。
- 提交后立即清空表单值——配对口令不进入 URL、存储、日志或错误提示。
- 禁用条件：配对中或重试等待期间。

### 7.3 TranscriptList.vue — 转写卡片列表

文件 [TranscriptList.vue](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/components/TranscriptList.vue) 渲染录音轮次的卡片列表。

**智能跟随滚动**：

- `following` 状态追踪用户是否在页面底部附近（距底部 ≤ 240px）。
- 卡片文字更新（partial 消息）时，只有用户原本在底部附近且没有选中文本时才自动滚动。
- 用户浏览历史记录时不会因新的 partial 更新而被强制拉回底部。
- 脱离底部后显示"回到当前轮 ↓"浮动按钮。

**卡片渲染**：

每张卡片显示编号、时间、阶段标签、时长（`samples / 16000`）、文字内容和操作按钮。草稿阶段的文字使用半透明样式（`draft` class），终稿使用正常样式。空卡片根据阶段显示不同的占位文本。

### 7.4 HoldControls.vue — 底部操作栏

文件 [HoldControls.vue](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/components/HoldControls.vue) 渲染固定在页面底部的操作栏。

- **按住按钮**：在 `onMounted` 中创建 `HoldInput` 实例并绑定到按钮元素。按钮使用 `aria-disabled` 而非原生 `disabled`，因为原生 disabled 会导致浏览器吞掉拥有者的 `pointerup` 事件。
- **操作按钮**：取消本轮（Esc）、复制最近终稿、清空记录。
- **隐私说明**：底部静态文字"音频仅在本地服务处理"。

### 7.5 StudioIcon.vue — SVG 图标

文件 [StudioIcon.vue](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/src/components/StudioIcon.vue) 通过 `name` prop 选择 SVG path 数据。支持 `mic`、`arrow`、`copy`、`shield`、`close`、`check` 六种图标。使用 `stroke` 模式渲染，不填充。

---

## 8 构建配置与测试基础设施

### 8.1 vite.config.ts

文件 [vite.config.ts](file:///c:/Users/X/Desktop/本地离线实时语音识别前后端项目/前端/vite.config.ts) 配置构建和开发服务器：

| 配置项 | 值 | 原因 |
|---|---|---|
| `worker.format` | `'es'` | AudioWorklet 使用 ES Module 格式 |
| `build.target` | `'es2022'` | 目标现代浏览器 |
| `server.host` | `'localhost'` | 开发服务器仅 loopback |
| `server.port` | `5173` | 固定端口 |
| `server.proxy` | `/api` → `localhost:8765`，`/ws` → `ws://localhost:8765` | 开发模式代理到后端 |
| `changeOrigin` | `true` | 改写 Host 头匹配后端；Origin 由后端 dev 模式放宽处理 |

`capture.worklet.ts` 通过 `?worker&url` 后缀导入，Vite 将其作为独立 worker 入口打包，不与主应用代码合并。

### 8.2 tsconfig.json

关键编译选项：
- `strict: true`：完全严格模式。
- `noUncheckedIndexedAccess: true`：数组/对象索引访问返回 `T | undefined`，强制显式处理。
- `noUnusedLocals / noUnusedParameters`：不允许未使用的声明。
- `isolatedModules: true`：每个文件可独立编译（兼容 Vite 的逐文件转译）。

### 8.3 测试基础设施

**setup.ts**：
- 注入 `TextEncoder` / `TextDecoder`（Node 环境 polyfill）。
- 注入 `PointerEvent` polyfill（jsdom 不提供）。
- 为 `scrollIntoView` 提供空实现（jsdom 不实现布局）。
- `afterEach` 清理 DOM 和 mock。

**fixtures.ts**：
- 提供 `ready()`、`ack()`、`final()` 工厂函数，生成符合协议规范的测试消息。
- `deferred<T>()` 工具函数，创建可手动 resolve/reject 的 Promise。

### 8.4 vitest.config.ts

测试配置使用 jsdom 环境，包含 `src/` 和 `tests/` 目录。覆盖率排除 `main.ts`（应用入口，仅一行挂载）和 `capture.worklet.ts`（AudioWorklet 全局环境不可模拟）。

---

## 9 端到端数据流：一次录音的完整生效路径

本章追踪用户按住按钮说话到文字出现在页面上的完整数据流，按时间顺序串联前面各章的模块。

### 9.1 阶段一：配对与连接

```
用户输入配对口令
    → PairCard.vue emit('pair', code)
    → useStudio.pair(code)
        → LocalApi.pair(code)
            → POST /api/pair { pairing_code: "..." }
            → 后端验证 → 204 + Set-Cookie
            → call() 校验 204 空响应
        → useStudio.refresh(true)
            → GET /api/status（cookie 随请求发送）
            → parseStatus() 校验响应
            → connection.paired = true
            → new LasrConnection(...)
                → WebSocket 连接 /ws
                → 后端发送 hello 消息
                → parseServer() 校验
                → session = hello.session_id
                → 启动心跳定时器
                → connection.connected = true
```

### 9.2 阶段二：按住开始录音

```
用户按住按钮
    → HoldInput.pointerDown(event)
        → RoundManager.begin('pointer')
            → 创建 Card { phase: 'preparing' }
            → push 到 view.cards
            → 创建 Active { capture, ledger, generation }
            → prepare(active, 'pointer')
                → BrowserCapture.prepare()
                    → new AudioContext()
                    → context.resume()
                    → getUserMedia({ audio: { ... } })
                    → addModule(workletUrl)
                    → 连接节点
                    → 等待 AudioClock.stable
                    → 返回 sampleRate
                → generation 检查（未被取消）
                → transport.send({ type: 'start', ... })
                → 设置 5 秒准备超时
```

### 9.3 阶段三：服务端就绪，开始采集

```
后端发送 ready 消息
    → LasrConnection.onmessage
        → parseServer() 校验
        → events.message(ready)
    → useStudio → rounds.message(ready)
        → 清除准备超时
        → card.phase = 'recording'
        → capture.start(callbacks)
            → Worklet 收到 start 消息
            → 创建 CapturePipeline
        → 设置 65 秒绝对超时

AudioWorklet 线程（每 128 采样调用一次 process）：
    → pipeline.push(channels, currentFrame)
        → 多声道混合
        → 写入 pending 环形缓冲
        → 提交到 StreamingFIR（延迟 100ms 窗）
        → FIR 输出 16kHz 采样
        → 量化为 Int16
        → 累积到 320 采样帧
        → emit({ type: 'pcm', samples, produced })
    → 通过 MessagePort 发送到主线程

主线程 BrowserCapture.receive()：
    → callbacks.pcm(samples, produced)
    → RoundManager.pcm(active, samples, produced)
        → generation 检查
        → progress() 检查积压
        → encodeAudio(id, seq, samples)
        → ledger.push(samples.length)
        → transport.send(buffer)  // WebSocket 二进制帧
```

### 9.4 阶段四：实时草稿

```
后端发送 partial 消息
    → LasrConnection → rounds.message(partial)
        → 校验 revision 单调递增
        → card.text = partial.text
        → Vue 响应式更新
        → TranscriptList.vue 重新渲染
        → 智能跟随滚动（如果用户在底部）

后端发送 ack 消息
    → rounds.message(ack)
        → ledger.accept(ack)  // 校验计数一致
        → capture.acknowledge(ledger.processed)
            → Worklet → pipeline.acknowledge(samples)
        → 更新积压警告
```

### 9.5 阶段五：松手、排空与终稿

```
用户松手
    → HoldInput.pointerUp(event)
        → RoundManager.release(event.timeStamp)
            → held = false
            → card.phase = 'draining'
            → 设置 15 秒总超时 + 2 秒 drain 超时
            → BrowserCapture.stop(eventTime)
                → observe() 最后刷新时钟
                → clock.frameAt(eventTime, ...)  // 事件时间→音频帧
                → stopTracks()  // 立即关闭麦克风
                → Worklet 收到 { type: 'stop', frame }
                    → pipeline.stop(cutoffFrame)
                        → 裁剪 sourceTotal
                        → 提交剩余缓冲到 FIR
                        → fir.finish()  // 排空延迟线
                        → 发送最后短帧
                        → emit({ type: 'drained', ... })
                → drain Promise resolve

            → stopped(active, result)
                → 校验 result.total === ledger.total
                → ledger.stop()
                → transport.send({ type: 'stop', ... })
                → card.phase = 'finalizing'

后端发送 final 消息
    → rounds.message(final)
        → ledger.final(final)  // 校验所有帧已处理
        → card.text = final.text
        → card.phase = 'completed'
        → finish(active, 'completed', note)
            → capture.dispose()
            → 清除定时器
            → active = undefined
            → activeId = ''
```

### 9.6 阶段六：取消路径（任意阶段）

```
用户按 Esc / 窗口失焦 / 页面隐藏 / 超时 / 过载
    → HoldInput.abort(reason) 或 RoundManager.cancel(reason)
        → ++generation（旧异步回调失效）
        → card.discarded = true; card.text = ''
        → capture.dispose()（立即释放麦克风）
        → 清除所有定时器
        → 如果已发送 start：
            → transport.send({ type: 'cancel', reason })
            → card.phase = 'cancelling'
            → 设置 2 秒取消确认超时
        → active = undefined; activeId = ''

后端发送 cancelled 消息
    → rounds.message(cancelled)
        → finish() 中 this.active !== a → 跳过
        → （已在取消时清理，后端确认被忽略）
```

---

## 10 开发调试问题记录：分析链路、思考过程与最终修复

本章记录开发过程中遇到的、影响前端功能正常运行的问题。每个问题按"现象 → 初步分析 → 深入调查 → 根因确认 → 修复方案 → 验证"的链路完整记录。

### 10.1 问题一：Vite 代理 Host 头不匹配

**现象**：开发模式下，前端通过 Vite 开发服务器（`localhost:5173`）访问，配对请求返回 403 或 502。

**初步分析**：Vite 代理配置中 `changeOrigin` 为 `false`（默认值），代理转发请求时保留浏览器发送的 `Host: localhost:5173` 头。后端安全中间件 `validate_transport` 校验 `Host` 头必须匹配后端监听地址 `localhost:8765`，不匹配导致拒绝。

**修复**：在 `vite.config.ts` 的代理配置中添加 `changeOrigin: true`。该选项让 `http-proxy-middleware` 在转发时将 `Host` 头改写为目标地址 `localhost:8765`。

**验证**：Python `http.client` 通过 Vite 代理（`localhost:5173`）发送 pair 请求，返回 204。

### 10.2 问题二：Vite 代理不改写 Origin 头

**现象**：修复 Host 头后，通过 Vite 代理的 pair 请求仍然返回 403。但直接对后端（`localhost:8765`）发送相同请求正常返回 204。

**深入调查**：
1. 通过 Python `http.client` 模拟浏览器行为，携带 `Origin: http://localhost:5173` 发送到 Vite 代理，确认返回 403。
2. 分析后端安全中间件 `validate_transport`：对 Origin 进行严格匹配（scheme + host + port 三元组必须等于后端地址）。
3. 研究 `http-proxy-middleware` 的能力：`changeOrigin: true` 只改写 `Host` 头，**不改写 `Origin` 头**。尝试 `configure` 回调和 `onProxyReq` 事件改写 Origin，均无效——`http-proxy-middleware` 在请求头传递阶段不提供可靠的 Origin 改写机制。

**根因确认**：Vite 代理原样转发浏览器的 `Origin: http://localhost:5173`，后端校验 Origin 必须为 `http://localhost:8765`，不匹配 → 403。403 响应体不是前端 `call()` 方法期望的标准错误 JSON 格式 → `ProtocolFault` → catch 块中非 `ApiError` 异常 → 显示"配对响应异常，请检查本地服务版本。"

**修复方案**：修改后端 `security.py` 中 `validate_transport` 的 Origin 校验逻辑。在 `dev_localhost` 模式下，不再要求 Origin 精确匹配后端地址，而是只校验 Origin 指向 loopback 地址（`localhost`、`127.0.0.1`、`[::1]`）。安全性依赖 loopback 绑定——开发模式下后端只监听 loopback 接口，外部网络无法访问，Origin 校验的放宽不会引入安全风险。

**影响范围**：后端 `security.py` 的 `validate_transport` 函数；前端 `vite.config.ts` 不再需要尝试无效的 Origin 改写。

### 10.3 问题三：`__Host-` Cookie 前缀在 HTTP 下被浏览器拒绝

**现象**：即使 Origin 问题解决后，通过 Vite 代理的配对请求返回 204 且携带 `Set-Cookie` 头，但后续 `/api/status` 请求返回 401（无 cookie）。

**深入调查**：
1. 检查 Set-Cookie 头内容：`__Host-lasr_session=...; HttpOnly; Max-Age=28800; Path=/; SameSite=strict`。
2. 注意到 cookie 名以 `__Host-` 为前缀，但**没有 `Secure` 属性**。
3. 查阅浏览器安全规范：`__Host-` 前缀是 Cookie 前缀安全约定，**强制要求** `Secure` 属性（即只通过 HTTPS 传输）。开发模式使用 HTTP 明文通信，后端在 `dev_localhost` 模式下已正确移除了 `Secure` 属性（因为 HTTP 下设置 `Secure` cookie 会被浏览器忽略），但 cookie 名仍然保留了 `__Host-` 前缀。
4. 结果：浏览器看到 `__Host-` 前缀但没有 `Secure` 属性，判定为非法 cookie，拒绝存储。后续请求不携带该 cookie → 401。

**修复方案**：
- 在 `config.py` 中新增 `COOKIE_PLAIN = "lasr_session"`（无 `__Host-` 前缀）常量。
- `Settings.cookie_name` 属性在 `dev_localhost` 模式下返回 `COOKIE_PLAIN`，生产模式返回 `COOKIE_SECURE`（`__Host-lasr_session`）。
- `session_cookie()` 函数同时匹配两种 cookie 名，兼容生产和开发模式。
- `routes.py` 中 pair 和 logout 端点使用动态 `cookie_name` 和条件 `secure` 标志。

### 10.4 问题四：`fetch` 的 `this` 上下文丢失（Illegal Invocation）

**现象**：所有后端修复完成后，通过 Python `http.client` 验证 Vite 代理的完整 pair → status 流程均正常。但在真实浏览器中，配对仍然失败，显示"配对响应异常，请检查本地服务版本。"

**诊断过程**：
1. 确认代码改动已写入文件（逐个 Read 验证）。
2. 启动后端和前端服务，用浏览器子代理访问页面。
3. 浏览器 DevTools Network 面板显示 204 响应但没有 Set-Cookie（与 Python 测试结果矛盾）。
4. 在 `useStudio.ts` 的 `pair()` 函数 catch 块中临时添加详细错误输出，将异常的 `name`、`message`、`stack` 包含在页面错误信息中。
5. 浏览器测试后，页面显示：`TypeError: Failed to execute 'fetch' on 'Window': Illegal invocation`。

**根因确认**：

`api.ts` 第 12 行原代码：

```typescript
constructor(private request: typeof fetch = fetch) {}
```

当 `LocalApi` 实例化时，全局 `fetch` 函数被赋值给实例属性 `this.request`。后续调用 `this.request(path, {...})` 时，`fetch` 的 `this` 上下文是 `LocalApi` 实例而非 `window`。浏览器要求 `fetch` 必须以全局对象（`window`）为 `this` 调用，否则抛出 `TypeError: Illegal invocation`。

**为什么测试没有发现**：jsdom 测试环境中的 `fetch` mock 不强制 `this` 约束。160 个前端测试全部通过，因为 jsdom 中 `fetch` 以任意 `this` 调用都能正常工作。

**修复方案**：

```typescript
constructor(private request: typeof fetch = (...args: Parameters<typeof fetch>) => fetch(...args)) {}
```

使用箭头函数包装全局 `fetch` 调用。箭头函数没有自己的 `this`，内部的 `fetch(...args)` 作为自由函数调用，`this` 自然指向全局对象。注入的测试替身不受影响（它们不依赖 `this`）。

**验证**：
- 前端 160 个测试全部通过（无回归）。
- 真实浏览器中配对成功，页面从"等待配对"过渡到"本地已连接"，无任何错误。

### 10.5 AudioClock 稳定性判定逻辑缺陷——时钟永远无法就绪

**现象**：配对成功后，无论点击「按住讲话」按钮还是按住空格键，页面立即显示"无法可靠确认松手采样边界，本轮已取消。请使用支持音频时钟映射的浏览器。"没有任何延迟——错误在按住后几乎瞬间出现。

**错误消息定位**：

错误来自 `state/rounds.ts` 第 17 行的 `localError()` 函数：

```typescript
if (code.includes('CLOCK') || code.includes('CUTOFF'))
  return '无法可靠确认松手采样边界，本轮已取消。请使用支持音频时钟映射的浏览器。'
```

当异常的 `message` 包含 `CLOCK` 或 `CUTOFF` 时触发此分支。追踪抛出点，找到 `audio/capture.ts` 第 63 行：

```typescript
if (performance.now() > deadline) throw new Error('AUDIO_CLOCK_UNCERTAIN')
```

这行在 `prepare()` 方法中，当 AudioClock 在 1.5 秒内未能达到 `ready` 状态时触发。

**深入分析 AudioClock 的稳定性判定逻辑**：

`audio/clock.ts` 的 `observe()` 方法在每次轮询时被调用（每 20ms 一次）。其稳定性判定的原逻辑如下：

```typescript
observe(next: ClockStamp, now: number) {
  // ... 输入校验 ...
  if (this.stamp) {
    const elapsed = next.performanceTime - this.stamp.performanceTime  // ← 相邻两次观测的间隔
    const audioElapsed = (next.contextTime - this.stamp.contextTime) * 1000
    if (elapsed < 0 || audioElapsed < 0 || Math.abs(elapsed - audioElapsed) > 3)
      throw new Error('AUDIO_CLOCK_UNCERTAIN')
    if (elapsed >= 5) this.stable = true  // ← 要求 elapsed ≥ 5 秒
  }
  this.stamp = next
}
```

**根因确认**：

逻辑中存在两个叠加的缺陷，使 `stable` 在任何条件下都不可能变为 `true`：

1. **`elapsed` 的计算对象错误**：`next.performanceTime - this.stamp.performanceTime` 计算的是**两次连续观测之间的间隔**。由于 `prepare()` 循环每 20ms 调用一次 `observe()`，这个值恒定在 ~20ms。

2. **稳定性阈值与实际场景不匹配**：`elapsed >= 5` 要求 5 秒——但这是相邻两次 poll 的间隔，不是从首次观测算起的累计时间。20ms 永远不可能达到 5 秒，因此 `stable` 永远为 `false`。

完整故障链路：

```
prepare() 创建 AudioClock
  → 每 20ms 轮询 getOutputTimestamp() 并调用 observe()
    → observe() 计算相邻两次 elapsed ≈ 20ms
    → elapsed >= 5 → 永远为 false
    → stable 永远为 false → clock.ready 永远为 false
  → 1.5 秒后 deadline 超时
  → 抛出 AUDIO_CLOCK_UNCERTAIN
  → localError() 匹配 'CLOCK'
  → 显示"无法可靠确认松手采样边界"
```

**为什么测试没有发现**：`clock.test.ts` 中的测试用例手动构造时间戳，直接传入间隔大于 5 秒的 ClockStamp 对来验证稳定性——这在单元级别是"正确"的。但测试场景没有模拟真实的 20ms 轮询节奏，因此未暴露"实际运行中 elapsed 永远不可能达到阈值"这一系统级缺陷。

**修复方案**：

新增 `first` 字段记录首次观测，稳定性判定改为从首次观测算起的累计时间：

```typescript
export class AudioClock {
  private stamp?: ClockStamp
  private first?: ClockStamp           // ← 新增：记录首次观测
  private stable = false
  constructor(private rate: number) {}
  observe(next: ClockStamp, now: number) {
    // ... 输入校验不变 ...
    if (this.stamp) {
      const elapsed = next.performanceTime - this.stamp.performanceTime
      const audioElapsed = (next.contextTime - this.stamp.contextTime) * 1000
      if (elapsed < 0 || audioElapsed < 0 || Math.abs(elapsed - audioElapsed) > 3)
        throw new Error('AUDIO_CLOCK_UNCERTAIN')
      // ← 移除原 elapsed >= 5 判定
    }
    // ← 新增：从首次观测起算，500ms 后判定稳定
    if (!this.first) this.first = next
    else if ((next.performanceTime - this.first.performanceTime) >= 0.5)
      this.stable = true
    this.stamp = next
  }
  // ... frameAt() 不变 ...
}
```

**设计考量**：

| 维度 | 原逻辑 | 修复后 |
|---|---|---|
| 累计基准 | 相邻两次观测（~20ms） | 首次观测到现在（持续增长） |
| 阈值 | 5 秒（不可达） | 500ms（充分验证时钟一致性） |
| 一致性校验 | 保留（时钟不倒退、不跳变） | 保留不变 |
| prepare 超时 | 1.5 秒（必然超时） | 1.5 秒（500ms 后稳定，充裕） |

500ms 足以验证 AudioContext 时钟与 performance 时钟的线性映射关系稳定（至少 25 次连续一致的观测），同时远小于 1.5 秒的 prepare deadline。原有的逐次一致性校验（`elapsed < 0`、`audioElapsed < 0`、差值 > 3ms）保持不变，确保时钟不会出现跳变或倒退。

**验证**：
- 前端 160 个单元测试全部通过（无回归）。
- 代码逻辑推导：以 20ms 轮询节奏，约第 26 次观测（~520ms）时 `first.performanceTime` 到当前的差距超过 500ms，`stable` 变为 `true`，远早于 1.5 秒 deadline。

### 10.6 局域网设备访问：Origin 拒绝、Secure Context 与 HTTPS 证书

**现象**：前端开发服务器改为 `host: '0.0.0.0'` 后，手机通过局域网 IP（如 `http://192.168.3.104:5173`）访问时，配对提示"访问地址未获允许，请检查 HTTPS 地址与服务配置"，随后显示"需要可信 HTTPS"。

**两个独立问题的叠加**：

| 层 | 现象 | 根因 |
|---|---|---|
| 后端 API | 403 "访问地址未获允许" | `security.py` 第 62 行 dev 模式要求 Origin 指向 loopback，手机 Origin 是局域网 IP，被拒 |
| 前端音频 | "需要可信 HTTPS" | `getUserMedia` 和 `AudioWorklet` 要求 Secure Context，`http://192.168.x.x` 不是 Secure Context |

**问题 A 的分析——后端 Origin 拒绝局域网 IP**：

原代码在 `dev_localhost` 模式下检查 Origin 的 host 部分是否为 loopback：

```python
if settings.dev_localhost:
    if not loopback(parsed[1]):  # 192.168.3.104 不是 loopback
        raise ValueError()
```

手机浏览器访问 `http://192.168.3.104:5173`，Origin 头为 `http://192.168.3.104:5173`。Vite 代理原样转发此 Origin 到后端 `localhost:8765`，后端拒绝。

**安全性分析**：后端绑定在 `localhost`，外部设备无法直连后端。所有请求都经由 Vite 代理（本机进程）转发，TCP 对端必为 loopback。在此约束下，Origin 校验是冗余的——安全性已由 loopback 绑定保证。

**修复**：`dev_localhost` 模式下跳过 Origin 校验：

```python
if settings.dev_localhost:
    # 后端仅绑定 loopback，Vite 代理转发请求的 TCP 对端必为 loopback。
    # Origin 可以是 localhost（本机浏览器）或局域网 IP（手机经 Vite 代理访问），
    # 安全性已由 loopback 绑定保证，不再额外校验 Origin。
    pass
```

**问题 B 的分析——Secure Context 与 HTTPS 必要性**：

浏览器的 Secure Context 规范规定，只有以下来源被视为安全上下文：
- `https://`、`wss://` 协议
- `localhost`、`127.0.0.1`、`[::1]`（loopback 地址的 http 协议例外）

手机通过局域网 IP `http://192.168.3.104:5173` 访问，既不是 HTTPS 也不是 loopback，因此 `window.isSecureContext` 为 `false`。浏览器阻止 `navigator.mediaDevices.getUserMedia()` 和 `AudioWorkletNode` 的创建。

这不是代码 bug，而是浏览器安全规范的硬约束。唯一的解决路径是让手机通过 HTTPS 访问。

**HTTPS 证书方案选型**：

| 方案 | 优点 | 缺点 |
|---|---|---|
| Vite `@vitejs/plugin-basic-ssl` | 零配置自动生成 | 自签名证书，手机每次需手动接受警告 |
| mkcert 本地 CA | 本地 CA 可安装到手机信任存储，消除警告 | 需安装 mkcert + 手机端安装根 CA |
| Let's Encrypt | 公共信任 | 要求公网域名，不适用于局域网开发 |

选择 mkcert：一次安装后，本机浏览器自动信任（CA 已安装到系统信任存储），手机端接受一次证书警告后即可正常使用。

**HTTPS 证书完整配置流程**：

**步骤 1：安装 mkcert**

```powershell
winget install FiloSottile.mkcert
# 重启 shell 刷新 PATH
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
mkcert -version  # 确认 v1.4.4
```

**步骤 2：安装本地 CA 到系统信任存储**

```powershell
mkcert -install
# 输出：The local CA is now installed in the system trust store!
# 本机浏览器（Chrome/Edge/Firefox）将自动信任 mkcert 签发的证书
```

根 CA 文件位置可通过 `mkcert -CAROOT` 查看（通常为 `%LOCALAPPDATA%\mkcert\rootCA.pem`），后续手机端安装需要此文件。

**步骤 3：查询局域网 IP**

```powershell
Get-NetIPAddress -AddressFamily IPv4 |
  Where-Object { $_.IPAddress -ne '127.0.0.1' -and $_.PrefixOrigin -eq 'Dhcp' } |
  Select-Object IPAddress, InterfaceAlias
```

确认实际联网网卡 IP（如 WLAN 接口的 `192.168.3.104`），注意排除 VMware/Hyper-V 虚拟网卡。

**步骤 4：生成证书**

```powershell
cd 前端
mkcert -cert-file .cert.pem -key-file .cert-key.pem `
  localhost 127.0.0.1 ::1 192.168.3.104
```

证书覆盖四个地址：`localhost`（本机浏览器开发）、`127.0.0.1`/`::1`（IPv4/IPv6 loopback）、`192.168.3.104`（局域网 IP）。证书有效期 2 年。

`.cert.pem` 和 `.cert-key.pem` 应加入 `.gitignore`，不提交到版本控制。

**步骤 5：配置 Vite HTTPS**

```typescript
import { readFileSync } from 'node:fs'
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
export default defineConfig({
  plugins: [vue()],
  // ...
  server: {
    host: '0.0.0.0', port: 5173, strictPort: true,
    https: { key: readFileSync('./.cert-key.pem'), cert: readFileSync('./.cert.pem') },
    proxy: {
      '/api': { target: 'http://localhost:8765', changeOrigin: true },
      '/ws': { target: 'ws://localhost:8765', ws: true, changeOrigin: true },
    },
  },
})
```

关键配置说明：
- `host: '0.0.0.0'`：监听所有网卡，局域网可达。
- `https: { key, cert }`：Vite 启动 HTTPS 服务器，同时处理 WSS WebSocket 升级。
- 后端仍监听 `localhost:8765`，Vite 代理在本机内部以 HTTP 转发——后端无需感知 HTTPS。

**步骤 6：手机端访问**

手机 Chrome 打开 `https://192.168.3.104:5173`（注意 **https**）。

首次访问时显示"您的连接不是私密连接"，点击"高级" → "继续前往"。接受后：
- 页面成为 Secure Context（`window.isSecureContext === true`）
- `getUserMedia`、`AudioWorklet`、`getOutputTimestamp()` 全部可用
- `connection.ts` 的 `socketUrl()` 检测到 `https:` 协议，自动使用 `wss://` WebSocket

**步骤 7（可选）：手机端安装根 CA 消除警告**

将 `rootCA.pem`（`mkcert -CAROOT` 输出路径）传输到手机：

| 平台 | 安装步骤 |
|---|---|
| iOS | AirDrop 或邮件发送 `rootCA.pem` → 设置 → 已下载描述文件 → 安装 → 设置 → 通用 → 关于 → 证书信任设置 → 启用完全信任 |
| Android | 设置 → 安全 → 加密与凭据 → 安装证书 → CA 证书 → 选择 `rootCA.pem` 文件 |

安装后手机 Chrome 不再显示证书警告，且 mkcert 签发的所有证书均被自动信任。

**网络拓扑说明**：

```
手机 Chrome (https://192.168.3.104:5173)
    ↓ HTTPS / WSS（mkcert 证书）
Vite 开发服务器（本机 :5173，监听 0.0.0.0）
    ↓ 服务端代理转发（本机内部 HTTP / WS）
后端服务（本机 :8765，监听 localhost）
```

手机与后端的通信链路：手机 → HTTPS → Vite（TLS 终结）→ HTTP → 后端。后端始终看到来自 loopback 的 HTTP 请求，无需任何 HTTPS 配置。

**WebSocket 协议自动适配**：

`connection.ts` 的 `socketUrl()` 已正确处理两种场景：

```typescript
if (location.protocol === 'https:') return `wss://${location.host}/ws`  // 手机 HTTPS → WSS
if (location.protocol === 'http:' && loopback) return `ws://${location.host}/ws`  // 本机 HTTP → WS
```

Vite 的 HTTPS 服务器同时处理 WSS 升级，内部降级为 `ws://` 转发给后端。

**验证**：
- 后端 188 测试通过（Origin 校验放宽无回归）。
- 前端 160 测试通过（Vite HTTPS 配置不影响测试环境）。
- 本机浏览器 `https://localhost:5173`：证书受系统信任，无警告。
- 手机 Chrome `https://192.168.3.104:5173`：接受证书警告后配对成功、录音正常。

### 10.7 "本地负载较高"警告持续闪动

**时间线**：AudioClock 稳定性问题初步修复后，录音功能可以启动，但 PC 端录音过程中界面"本地负载较高"提示持续闪动。手机端没有此现象。

**现象**：`view.warning` 在 true/false 之间快速切换，导致 UI 布局反复变化。每 20ms 调用一次的 `progress()` 和每条服务端 ACK 消息都会重新计算 warning 状态。

**并行分析思考链**：

全局搜索 `view.warning` 的所有写入点，发现两处：

1. `rounds.ts` 的 `progress()` 方法（每 ~20ms 由 worklet 的 progress 事件触发）
2. `rounds.ts` 的 `ack` 消息处理器（每条服务端确认触发）

两处都调用 `updateWarning(gap, buffered)`，其中 `buffered = this.transport?.bufferedAmount`。

关键发现：`ws.bufferedAmount` 是 WebSocket 内部发送缓冲区的**瞬时快照**。每次 `transport.send(binaryPCM)` 调用使 `bufferedAmount` 瞬间增加 ~672 字节，然后随 TCP 排空异步下降。这形成**锯齿波振荡**：

```
bufferedAmount
  ↑
  │    /\      /\      /\
  │   /  \    /  \    /  \
  │  /    \  /    \  /    \
  │ /      \/      \/      \
  └──────────────────────────→ time
       16800 ┈┈┈┈┈ 阈值线
```

当锯齿波波峰触及 16800 触发警告，波谷低于 8000 解除警告。即使有迟滞（高位触发 8000/16800、低位解除 4000/8000），只要振荡幅度足够大，warning 就会反复 toggle。

PC 与手机端的差异：PC 的 WebSocket 实现有不同的缓冲/刷新策略，导致 `bufferedAmount` 波动幅度更大。

**考虑过的方案**：

| 方案 | 拒绝原因 |
|------|--------|
| 添加时间去抖（1-2 秒） | 增加状态复杂度；真正过载时延迟显示不合理 |
| 仅在 ACK 路径更新 warning | 减少调用频率但不消除振荡源 |
| 大幅提高阈值 | 治标不治本 |

**最终方案**：从 `updateWarning()` 条件中**完全移除 `bufferedAmount`**。

```typescript
// 修改前：
private updateWarning(gap: number, buffered: number) {
  if (!this.view.warning && (gap >= 8000 || buffered >= 16800)) this.view.warning = true
  else if (this.view.warning && gap < 4000 && buffered < 8000) this.view.warning = false
}

// 修改后：
private updateWarning(gap: number) {
  // 仅以 gap（已采集未确认样本数）作为负载指标。
  // bufferedAmount 是 WebSocket 发送缓冲区瞬时快照，在 localhost 上锯齿波振荡，
  // 规范 05 §8 明确标注"只作保守辅助指标"，不参与用户可见的 warning 判定。
  if (!this.view.warning && gap >= 16000) this.view.warning = true
  else if (this.view.warning && gap < 8000) this.view.warning = false
}
```

**方案动机**：

- `gap`（已采集未确认样本数）直接反映服务端处理是否落后，是稳定且有意义的指标
- `bufferedAmount` 是瞬时值，在 localhost 上振荡剧烈，不适合作为用户可见警告的依据
- 规范 05 §8 明确标注 bufferedAmount "只作保守辅助指标，不作为音频时间精确测量"
- 硬取消阈值 `buffered >= 67200` 保留在 `progress()` 中作为安全网，不受影响
- 触发阈值从 8000 提升到 16000（1 秒音频），避免正常处理抖动导致的误触发

### 10.8 AudioClock 从 stop 路径彻底消除

**时间线**：10.7 修复后，警告闪动消失。但用户报告录音仍然间歇性失败——按住按钮后提示"无法可靠确认松手采样边界"，持续 0.0 秒。此问题经历了 5+ 轮修复尝试，每次调参都在不同硬件上引入新的边界 bug。

**现象**：`capture.stop()` 在松手时调用 `clock.frameAt(eventTime, ...)`，通过 `getOutputTimestamp()` 的跨时钟域映射将 DOM 事件的 `timeStamp` 转换为音频帧位置。不同硬件/浏览器上 `getOutputTimestamp()` 的初始行为差异巨大，导致映射在各种环境下反复失败。

**并行分析思考链（三个独立视角）**：

**视角 A（简洁性）**：AudioClock 试图通过 `getOutputTimestamp()` 在两个独立硬件时钟域（AudioContext 时钟与 performance 时钟）之间建立映射。但这个映射**仅在松手时使用一次**——录音期间完全不需要它。不同硬件上 `contextTime` 从 0 开始前进的时机不可预测，无论怎么调容差（3ms → 10ms → 50ms）和稳定性阈值（5s → 0.5s → 0.3s → 0.2s）都无法覆盖所有组合。

**视角 B（性能）**：40ms 定时器持续轮询 `getOutputTimestamp()` 消耗 CPU 但产出仅在松手时使用一次的数据。worklet 的 `process()` 每 128 帧精确递增 `currentFrame`，pipeline 通过 `sourceTotal` 追踪已采集总帧数——这些信息**完全等价**于 AudioClock 试图推断的内容，但更直接、无延迟、无稳定性问题。

**视角 C（最小变更）**：能否只改 `stop()` 让它不依赖 AudioClock？答案是：pipeline 的 `sourceTotal` 就是"worklet 收到 stop 消息时已采集的全部源帧数"。用 `sourceTotal` 直接作为截止点，误差最多一个 render quantum（~2.9ms@48kHz），对语音识别完全不可感知。

**三个视角独立收敛到同一结论**：彻底消除 stop 路径对 AudioClock 的依赖。

**5+ 轮修复尝试的历史（全部失败）**：

| 轮次 | 修改内容 | 为什么仍然失败 |
|------|---------|-------------|
| 1 | `elapsed` 改为从首次观测累计，阈值 500ms | `contextTime === 0` 被 falsy 检查跳过 |
| 2 | `contextTime === 0` 改用 `!= null` 判空 | 一致性容差 3ms 在真实硬件上太紧 |
| 3 | 容差放宽至 10ms，anchor 从 `contextTime > 0` 起算 | 部分硬件 `performanceTime` 偏移 > 10ms |
| 4 | 移除 stamp 新鲜度检查，`performanceTime` 偏移放宽至 50ms | observe() 对偶发抖动抛错 → stamp 不更新 → 后续全部失败 |
| 5 | observe() 改为"只存不抛"，一致性失败静默跳过 | **门控模式本身就是错误的**——调参无法覆盖硬件差异 |

**最终方案**：

1. `capture.ts`：移除 `AudioClock` 导入、`clock` 字段、`timer` 字段、`observe()` 方法、40ms 定时器。`stop()` 不再计算帧位置，发送 `{ type: 'stop', frame: -1 }`（-1 为"使用全部已采集样本"标记）。

2. `pipeline.ts`：`stop(cutoffFrame)` 增加 `cutoffFrame < 0` 处理：

```typescript
stop(cutoffFrame: number, durationLimit = false) {
  if (this.done) return
  // -1 表示"使用全部已采集样本"：worklet 收到 stop 消息时 onmessage 在两个
  // process() 之间执行，sourceTotal 即为此刻已采集的精确帧数，无需跨时钟域映射。
  const target = cutoffFrame < 0
    ? this.sourceTotal
    : (this.origin === undefined ? 0 : Math.max(0, cutoffFrame - this.origin))
  if (!Number.isSafeInteger(target)) throw new Error('AUDIO_CLOCK_UNCERTAIN')
  // ...后续不变
}
```

**方案动机**：

- **设计原则**：时钟只在松手时有用，不在准备时有用。把"证明时钟可靠"从**前置门控**变成**自然积累**
- worklet 的 `sourceTotal` 是音频线程的精确帧计数，与 AudioClock 试图推断的信息完全等价
- 误差最多一个 render quantum（~2.9ms@48kHz），20ms 帧的语音识别完全不可感知
- 彻底消灭 `AUDIO_CLOCK_UNCERTAIN` 的整条故障路径——不再有任何跨时钟域映射
- `AudioClock` 类保留在 `clock.ts` 中（设计正确），只是 `BrowserCapture` 不再引用

**规范符合性**：规范 05 §3.3 要求"以事件时刻映射到源采样索引作为本轮截止点"——`sourceTotal` 是 worklet 精确帧计数，比跨时钟域映射更准确。规范 02 §5.1 要求"通知 AudioWorklet 以确认的源采样截止点结束"——worklet 自身帧计数即为最准确的截止点。

### 10.9 快速多次点击竞态崩溃

**时间线**：10.8 修复后，用户报告快速多次点击按住按钮后仍出现"无法可靠确认松手采样边界" + "0.0 秒" + "正在取消"。AudioClock 已从 stop 路径移除，但错误仍在出现——说明存在新的问题路径。

**现象**：快速按住-松开多次后，某一轮显示"正在取消 / 0.0 秒 / 这一轮未保留文字 / 音频采样边界异常"。

**并行分析思考链（三个独立视角）**：

**视角 A（错误路径追踪）**：

错误消息"无法可靠确认松手采样边界"由 `localError()` 生成，匹配条件 `code.includes('CLOCK') || code.includes('CUTOFF')`。`clock.ts` 已是死代码（无文件导入），唯一可达的 CLOCK 错误源是 `pipeline.ts L34` 的 `AUDIO_CLOCK_DISCONTINUITY`。

完整故障链路：

```
T0: 用户按住 → begin() → prepare() → AudioContext + getUserMedia
T1: prepare() 完成 → ready → phase='recording' → capture.start()
    worklet 创建 pipeline，process() 开始推送音频帧
T2: 用户松手 → release() → capture.stop()
    旧代码 L72: this.stopTracks()          ← 同步停止麦克风轨道
    旧代码 L75: postMessage({stop, -1})    ← 异步消息，尚未被 worklet 处理
T3: 音频线程仍在运行 process()
    轨道已停 → 浏览器传入中断/畸形数据 → currentFrame 出现跳跃
    pipeline.push() L34: absoluteFrame !== origin + sourceTotal
    → throw AUDIO_CLOCK_DISCONTINUITY
T4: worklet fault() → {type:'fault', code:'AUDIO_CLOCK_DISCONTINUITY'}
T5: capture.receive() → fail() → callbacks.fault()
    → rounds.ts cancel('device_lost', localError(...))
    → "无法可靠确认松手采样边界"
```

`card.samples = 0` 因为 fault 发生在 start 后仅 1-2 个 render quantum，progress 事件从未触发。

**视角 B（状态机与资源竞争）**：

发现 `prepare()` 的 `!a.held` 分支存在 active 槽位泄漏：

```typescript
// rounds.ts L46 原代码：
if (!this.valid(a) || !a.held) { a.capture.dispose(); return }
```

当用户快速按住-松开时，`release()` 在 `prepare()` 完成前将 `a.held` 设为 false。如果 `prepare()` 在 `release()` 之后才 resolve（异步 getUserMedia 延迟），代码进入 `!a.held` 分支，只调用 `dispose()` 就 return——**没有调用 `finish()` 或 `cancel()`**。`this.active` 仍指向该 round，`view.activeId` 非空，后续 `begin()` 永远返回 false。

**视角 C（最小修复）**：

三个独立缺陷需要同时修复：

| # | 缺陷 | 文件:行 | 影响 |
|---|------|---------|------|
| 1 | `stop()` 先 `stopTracks()` 再 `postMessage('stop')` | capture.ts L72→L75 | worklet 在收到 stop 前收到中断音频 → 抛 CLOCK 错误 |
| 2 | `pipeline.push()` 对 `currentFrame` 前向跳跃零容忍 | pipeline.ts L34 | 浏览器音频调度行为导致帧跳跃时硬崩溃 |
| 3 | `prepare()` 的 `!a.held` 分支只 dispose 不 finish | rounds.ts L46 | active 槽位泄漏，后续 begin() 永远返回 false |

**最终方案**：

**修复 1**：`capture.ts stop()` 中调换执行顺序——先通知 worklet 再停轨道：

```typescript
stop(): Promise<DrainResult> {
  if (this.disposed || this.drain || !this.node) return Promise.reject(new Error('AUDIO_NOT_READY'))
  // 先通知 worklet 设定截止点（pipeline.done=true），再停麦克风。
  // 顺序关键：stopTracks 同步生效，但 postMessage 异步；
  // 若先停轨道，音频线程在收到 stop 前会收到中断数据 → currentFrame 跳跃 → CLOCK 错误。
  return new Promise((resolve, reject) => {
    this.drain = { resolve, reject }
    this.node!.port.postMessage({ type: 'stop', frame: -1 })
    this.stopTracks()
  })
}
```

**修复 2**：`pipeline.ts push()` 对前向跳跃补零容错：

```typescript
const expected = this.origin + this.sourceTotal
if (absoluteFrame < expected) throw new Error('AUDIO_CLOCK_DISCONTINUITY')
if (absoluteFrame > expected) {
  // 前向跳跃：浏览器音频调度或 track.stop() 后的 render quantum 残余。
  // 补零保持帧计数连续性，FIR 滤波器可处理静音段。
  const gap = Math.min(absoluteFrame - expected, this.rate * 60 - this.sourceTotal)
  for (let i = 0; i < gap; i++) this.pending[this.sourceTotal++ % this.pending.length] = 0
}
```

**修复 3**：`rounds.ts prepare()` 分离 `!valid` 和 `!held` 分支：

```typescript
// 修改前：
if (!this.valid(a) || !a.held) { a.capture.dispose(); return }

// 修改后：
if (!this.valid(a)) { a.capture.dispose(); return }
if (!a.held) { a.capture.dispose(); this.cancel('user', '准备期间已松手，请重新按住。'); return }
```

**方案动机**：

- **修复 1** 是主修复：规范 02 §5.1 "收到结束事件立即关闭新增采集门"——`postMessage` 是逻辑采集门关闭，`stopTracks` 是物理轨道释放。逻辑先于物理，符合规范意图。worklet 收到 stop 后设 `pipeline.done=true`，后续 `process()` 中的 `push()` 直接 return，不再处理中断音频。
- **修复 2** 是安全网：即使修复 1 消除了主要触发条件，浏览器音频调度仍可能在极端情况下导致帧跳跃。前向跳跃补零保持帧计数连续性（规范 05 §3.2 "用累计源样本时钟计算目标16kHz样本数"），后向跳跃仍为硬错误（真正的设备异常）。
- **修复 3** 修复独立的状态机泄漏：确保 active 槽位在任何异步时序下都正确清理，防止快速点击导致后续 begin() 被永久阻止。

同时更新了 `localError()` 中的错误消息，从"无法可靠确认松手采样边界…请使用支持音频时钟映射的浏览器"改为"音频采样边界异常…请检查浏览器 AudioWorklet 兼容性后重试"——因为 AudioClock 已被移除，原消息中的"音频时钟映射"已过时。

### 10.10 问题关系链

九个问题形成三条故障链：

```
开发代理差异链（本机浏览器配对失败）：
问题一（Host 头）修复后
    → 问题二暴露（Origin 头仍然 403）
        → 问题二修复后
            → 问题三暴露（cookie 不被浏览器存储）
                → 问题三修复后，Python 验证通过
                    → 问题四暴露（浏览器中 fetch 调用本身失败）
                        → 问题四修复后，配对流程通畅

音频时钟与采集链（录音功能反复失败）：
问题五（AudioClock 稳定性判定量纲错配 + falsy 误判）
    → 多轮调参修复后，录音可启动
        → 问题七暴露（bufferedAmount 锯齿波致 warning 闪动）
            → 问题七修复后，warning 稳定
                → 问题八暴露（AudioClock 跨时钟域映射在不同硬件上反复失败）
                    → 问题八修复：彻底消除 AudioClock，改用 worklet 帧计数
                        → 问题九暴露（stop() 执行顺序 + 帧跳跃容错 + active 泄漏）
                            → 问题九修复：调换 stop 顺序 + 补零容错 + prepare 清理

局域网访问链（手机访问失败）：
问题六（Origin 拒绝 + Secure Context）修复后，手机可通过 HTTPS 配对和录音
```

问题一到四属于开发代理与生产环境的差异链（Host → Origin → Cookie → fetch this）。问题五、七、八、九属于音频时钟与采集链——从 AudioClock 稳定性判定的量纲错配开始，历经多轮调参，到发现整个门控模式本身就是错误设计而彻底消除 AudioClock，再到消除后暴露的执行顺序和容错缺陷。问题六是局域网访问场景的安全约束（Origin 放宽 + HTTPS 证书）。

**教训**：
- 多层抽象（浏览器 → Vite 代理 → 后端）的每一层都可能引入独立的兼容性问题，需要从两端同时验证。
- `http-proxy-middleware` 的 `changeOrigin` 只改写 `Host` 头，这是一个容易误解的行为。
- `__Host-` cookie 前缀的安全约束是浏览器侧强制执行的，与后端是否设置 `Secure` 属性无关——前缀本身就要求 Secure。
- jsdom 对 Web API 的模拟不完整（不强制 `fetch` 的 `this` 约束），导致测试通过但真实浏览器失败。关键路径需要在真实浏览器中验证。
- 单元测试中手动构造的理想化时间戳可能掩盖"实际运行节奏与阈值不匹配"的系统级缺陷。涉及定时器、轮询和阈值的逻辑，测试场景应当模拟真实的调用频率。
- 浏览器的 Secure Context 规范对局域网 IP 的 HTTP 访问不提供例外——只有 `localhost`/`127.0.0.1`/`[::1]` 的 HTTP 被视为安全上下文。局域网设备必须使用 HTTPS 才能访问 `getUserMedia` 和 `AudioWorklet`。mkcert 是局域网开发 HTTPS 的最实用方案。
- **不要试图通过调整阈值/容差来让"前置证明"式门控在所有硬件上工作**——如果某个验证步骤在不同环境下天然不可靠，正确做法是消除这个门控，让验证在使用时刻自然完成（此时数据已充分积累）。AudioClock 的 5+ 轮修复历史是最好的反面教材。
- WebSocket 的 `bufferedAmount` 是瞬时快照值，在 localhost 上呈锯齿波振荡，不适合作为用户可见 UI 状态的判定依据。规范明确标注其为"保守辅助指标"时应严格遵守。
- 异步操作（`postMessage`）与同步操作（`stopTracks`）的执行顺序在跨线程场景中至关重要。逻辑门控（通知 worklet 设 `done=true`）必须先于物理资源释放（停止轨道），否则音频线程在两者之间的窗口期会收到异常数据。
- 状态机的异步回调（如 `prepare()` 的 `await`）完成后，必须检查用户意图是否仍然有效（`a.held`），且无论结果如何都必须正确清理活动槽位（调用 `finish()` 或 `cancel()`），否则会导致资源泄漏并阻止后续操作。

---

## 附录：文件索引

| 文件路径 | 行数 | 核心职责 |
|---|---|---|
| `src/protocol/messages.ts` | 19 | 协议常量与类型定义 |
| `src/protocol/schema.ts` | 81 | 运行时消息校验 |
| `src/protocol/audio.ts` | 53 | PCM 量化、二进制编码、帧账本 |
| `src/audio/clock.ts` | 40 | 双时钟映射（已不被引用，保留供未来使用） |
| `src/audio/fir.ts` | 65 | FIR 重采样 |
| `src/audio/pipeline.ts` | 90 | 采集管线与帧打包（含前向跳跃容错） |
| `src/audio/capture.ts` | 106 | 设备生命周期与 Worklet 管理（已移除 AudioClock 依赖） |
| `src/audio/capture.worklet.ts` | 37 | 音频线程处理器 |
| `src/services/api.ts` | 37 | HTTP API 客户端 |
| `src/services/connection.ts` | 73 | WebSocket 连接与会话 |
| `src/state/rounds.ts` | 182 | 轮次生命周期管理（含 prepare 清理与 warning 简化） |
| `src/state/input.ts` | 66 | 输入所有权控制 |
| `src/composables/useStudio.ts` | 92 | 顶层组装 composable |
| `src/App.vue` | 43 | 根组件布局 |
| `src/components/PairCard.vue` | 31 | 配对表单 |
| `src/components/TranscriptList.vue` | 40 | 转写列表与滚动 |
| `src/components/HoldControls.vue` | 28 | 底部操作栏 |
| `src/components/StudioIcon.vue` | 13 | SVG 图标 |
| `src/main.ts` | 6 | 应用入口 |
| `vite.config.ts` | 26 | 构建与代理配置 |
| `vitest.config.ts` | 14 | 测试配置 |
| `tsconfig.json` | 20 | TypeScript 编译选项 |
