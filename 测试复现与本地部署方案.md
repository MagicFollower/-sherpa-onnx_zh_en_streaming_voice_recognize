# 测试复现与本地部署方案

## 一、模型方案与必备文件

### 1.1 模型概述

本项目使用 **sherpa-onnx-streaming-zipformer-bilingual-zh-en** 作为离线语音识别引擎。该模型由 [k2-fsa](https://github.com/k2-fsa) 团队发布，基于 Zipformer 架构，支持中英双语实时流式识别，通过 [sherpa-onnx](https://github.com/k2-fsa/sherpa-onnx) 推理框架加载运行，无需联网。

| 属性 | 值 |
|------|----|
| 模型名称 | sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20 |
| HuggingFace 仓库 | `csukuangfj/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20` |
| 锁定 commit | `98590b7ed6443e77b714204da2757d75e1a642f4` |
| 推理框架 | sherpa-onnx 1.13.8 |
| 量化方式 | encoder / joiner 使用 int8 量化，decoder 保持 float32 |
| 解码策略 | greedy_search |
| 流式参数 | decode_chunk_len=32, T=39, left_context=64/32/16/8/32 |
| 采样率 | 16 kHz（前端 FIR 重采样至 16 kHz 后送入） |

### 1.2 必备文件清单

模型文件存放于项目根目录 `models/zipformer-bilingual-98590b7e/`，共 4 个文件，合计约 190 MB：

| 文件名 | 大小 | 说明 | SHA256 |
|--------|------|------|--------|
| `encoder-epoch-99-avg-1.int8.onnx` | 173.5 MB | 编码器（int8 量化），流式特征提取与上下文建模 | `8fa764187a261844f859d7143ebaa563af5d10adfece4c18a8f414c88cba2a9b` |
| `decoder-epoch-99-avg-1.onnx` | 13.2 MB | 解码器（float32），将编码器输出映射到 token 概率 | `2e3b5ec371f8899ee6acd829fd753ba45772df57a91bdf37cde3136354e7db7d` |
| `joiner-epoch-99-avg-1.int8.onnx` | 3.1 MB | 联结器（int8 量化），融合编码器与解码器输出做 token 判定 | `1ed689c5ed19dbaa725d9d191bb4822b5f4855a39e1ffd28cbc1f340d25b2ee0` |
| `tokens.txt` | 55 KB | 词表文件，BPE token 到整数 ID 的映射 | `a8e0e4ec53810e433789b54a5c0134a7eaa2ffca595a6334d54c00da858841d3` |

所有 ONNX 文件同时通过 HuggingFace LFS SHA256 与本地 SHA256 双重校验，确保来源一致。

### 1.3 下载方式

**方式一：项目内置脚本自动下载（推荐）**

后端自带 `prepare_models.py` 脚本，从 HuggingFace 锁定 commit 原子下载全部文件，下载过程中校验长度与 SHA256，校验失败不启用文件：

```powershell
# 确保后端依赖已安装（sherpa-onnx、onnx）
cd 后端
.venv\Scripts\python.exe -m lasr prepare-models

# 同时下载官方演示音频（可选，仅用于冒烟测试）
.venv\Scripts\python.exe -m lasr prepare-models --audio
```

脚本行为：
- 下载中断只留 `.partial` 文件，不会污染目标目录
- 完整目录最后原子重命名生效
- 已存在的已验证目录仅复核不覆盖
- 所有网络请求强制 HTTPS，拒绝非 TLS 重定向

**方式二：手动从 HuggingFace 下载**

如果网络环境需要代理或手动操作，可直接从以下 URL 下载四个文件到 `models/zipformer-bilingual-98590b7e/`：

```
https://huggingface.co/csukuangfj/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20/resolve/98590b7ed6443e77b714204da2757d75e1a642f4/encoder-epoch-99-avg-1.int8.onnx
https://huggingface.co/csukuangfj/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20/resolve/98590b7ed6443e77b714204da2757d75e1a642f4/decoder-epoch-99-avg-1.onnx
https://huggingface.co/csukuangfj/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20/resolve/98590b7ed6443e77b714204da2757d75e1a642f4/joiner-epoch-99-avg-1.int8.onnx
https://huggingface.co/csukuangfj/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20/resolve/98590b7ed6443e77b714204da2757d75e1a642f4/tokens.txt
```

下载完成后运行校验：

```powershell
cd 后端
.venv\Scripts\python.exe -m lasr prepare-models --verify-only
# 成功输出 JSON（含 revision 和 metadata）；校验失败报错并拒绝启动
```

### 1.4 下载脚本源码位置

| 文件 | 职责 |
|------|------|
| `后端/lasr/artifacts.py` | 固定模型来源、commit、文件名与大小的常量定义；本地校验逻辑（长度 + SHA256 双重验证）；manifest 加载 |
| `后端/lasr/prepare_models.py` | 联网下载脚本：从 HuggingFace API 获取文件树，逐文件原子下载并校验，生成 manifest.json |
| `models/zipformer-bilingual-98590b7e/manifest.json` | 本地制品清单：记录每个文件的 URL、大小、本地 SHA256、上游 LFS SHA256、编码器元数据 |

---

## 二、模型热替换可行性分析

**结论：当前项目不支持热替换为其他模型。** 整个推理链路从制品校验、适配器参数到文件命名均硬编码绑定到唯一的 Zipformer 双语模型。即使通过 `--model-dir` 指向其他目录，也必须在代码中同步修改多处硬编码常量才能通过校验。

### 2.1 硬编码绑定清单

以下代码位置将模型来源、架构参数和文件名写死为常量，任何替换都需逐一修改：

| 文件 | 位置 | 硬编码内容 | 影响 |
|------|------|-----------|------|
| `artifacts.py` L12-13 | `REPO` / `REVISION` | HuggingFace 仓库名与 commit hash | `load_manifest()` 校验来源版本，不匹配直接拒绝 |
| `artifacts.py` L15-20 | `FILES` | 4 个文件名及其精确字节数 | `load_manifest()` 校验文件组合与大小，多一个少一个都报错 |
| `artifacts.py` L71 | ONNX LFS SHA256 比对 | 本地摘要必须等于上游 LFS 摘要 | 不同模型的哈希完全不同 |
| `recognizer.py` L15-18 | `CHUNK_SHIFT=32` / `CHUNK_SIZE=39` / `FEATURE_SHIFT=160` / `FEATURE_WINDOW=400` | 流式解码适配器参数 | L34-36 显式校验 `decode_chunk_len` 和 `T` 必须等于 32/39 |
| `recognizer.py` L40-42 | 文件名硬编码 | encoder/decoder/joiner 的精确文件名 | `sherpa_onnx.OnlineRecognizer.from_transducer()` 的入参写死 |
| `recognizer.py` L43-46 | 推理参数 | `sample_rate=16000`, `feature_dim=80`, `model_type="zipformer"`, 端点检测阈值 | 不同架构的模型（如 Whisper、Conformer）无法被 `from_transducer` 加载 |
| `prepare_models.py` L99 | `FILES.items()` | 下载循环只遍历固定的 4 个文件 | 自动下载脚本无法适配其他模型 |

### 2.2 校验链路追踪

从 CLI 到推理的完整校验路径，每一步都阻止非目标模型：

```
CLI --model-dir → Settings.model_dir
  → supervisor.py 传入 worker_main(directory=...)
    → Recognizer.__init__(directory)
      → load_manifest(directory)              ← 校验 repo/revision/FILES/sha256
        → manifest["repo"] != REPO            → 拒绝
        → manifest["revision"] != REVISION    → 拒绝
        → set(manifest["files"]) != set(FILES) → 拒绝
        → 文件大小不符                         → 拒绝
        → SHA256 不符                          → 拒绝
      → encoder_metadata 检查                  ← 校验 chunk 参数
        → decode_chunk_len != "32"             → 拒绝
        → T != "39"                            → 拒绝
      → sherpa_onnx 加载                       ← 只支持 transducer 架构
```

### 2.3 替换为同架构不同权重的可行性

如果新模型**仍然是 sherpa-onnx 的 streaming transducer (Zipformer) 架构**，且 `decode_chunk_len=32`、`T=39`、`feature_dim=80`、`sample_rate=16000` 均相同，理论上可以通过以下步骤替换：

1. 修改 `artifacts.py` 中的 `REPO`、`REVISION`、`FILES` 常量
2. 修改 `recognizer.py` 中的文件名硬编码（如果新模型文件名不同）
3. 为新模型编写或更新 `manifest.json`（包含正确的 SHA256）
4. 运行 `prepare-models` 下载并校验
5. 重启服务（**不支持运行时热替换**，worker 进程只在启动时加载一次模型）

**但当前代码没有任何配置化或插件化机制来支持这一流程**——每次换模型都需要修改 Python 源码中的常量。

### 2.4 替换为不同架构模型的可行性

| 目标架构 | 可行性 | 原因 |
|----------|--------|------|
| 其他 Zipformer 变体（不同量化/层数） | 需改代码 | chunk 参数可能不同，`recognizer.py` L34-36 硬编码校验 |
| sherpa-onnx 的其他 streaming transducer | 需改代码 | `from_transducer()` API 可加载，但文件名和 manifest 需改 |
| sherpa-onnx 的 non-streaming 模型 | 不可行 | 需要完全不同的解码循环（非流式），当前 `StreamAdapter` 架构不兼容 |
| Whisper / Paraformer / 其他框架 | 不可行 | 需要重写整个 `recognizer.py`、`worker.py`、音频协议适配层 |

### 2.5 设计理由

这种强绑定是有意为之的设计选择，而非遗漏：

- **可复现性**：冻结模型版本确保测试结果可复现，避免"换了模型不知道哪个版本出的问题"
- **安全约束**：`load_manifest()` 的严格校验防止加载被篡改的模型文件
- **协议耦合**：前端 FIR 重采样到 16kHz、PCM int16 量化、20ms 帧编码，全部针对当前模型的输入要求定制
- **端点检测参数**：`rule1_min_trailing_silence=2.4`、`rule2=1.2`、`rule3=300` 是针对该模型的静音检测调优值
- **离线交付场景**：本项目定位为局域网离线工具，不需要运行时动态切换模型

---

## 三、环境前置

```
项目根目录: c:\Users\X\Desktop\本地离线实时语音识别前后端项目
Python:     3.11（已安装在 .venv 中）
Node:       ≥22.12.0
模型:       models/zipformer-bilingual-98590b7e/ （已下载，manifest 已校验）
```

## 四、依赖安装（首次或清洁环境）

### 后端

```powershell
# 方式一：使用项目自带的环境准备脚本（推荐，隔离缓存到 .cache/backend）
cd 后端
python ..\tools\prepare_env.py

# 方式二：手动（如果 .venv 已存在且依赖完整）
cd 后端
.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

验证依赖完整性：

```powershell
后端\.venv\Scripts\python.exe -m pip check
后端\.venv\Scripts\python.exe -c "import sherpa_onnx; print(sherpa_onnx.__version__)"
# 应输出 1.13.8
```

### 前端

```powershell
cd 前端
npm install
```

## 五、模型校验

模型已下载。每次启动前可验证完整性：

```powershell
cd 后端
.venv\Scripts\python.exe -m lasr prepare-models --verify-only
# 成功输出 JSON，包含 revision 和 metadata；校验失败会报错并拒绝启动
```

## 六、本地运行自动化测试

### 后端测试（188 测试）

```powershell
cd 后端
.venv\Scripts\python.exe -m pytest tests/ -v
# 预期: 188 passed, 3 warnings

# 带覆盖率
.venv\Scripts\python.exe -m pytest tests/ --cov=lasr --cov-report=html
# 覆盖率报告: .cache/backend/coverage-html/index.html

# Lint
.venv\Scripts\python.exe -m ruff check lasr/ tests/
```

### 前端测试（160 测试）

```powershell
cd 前端
npx vitest run
# 预期: 8 passed (8), 160 passed (160)

# 带覆盖率
npx vitest run --coverage
# 覆盖率报告: 前端/coverage/

# TypeScript 类型检查
npx vue-tsc --noEmit
```

## 七、构建前端

后端生产模式从 `前端/dist/` 提供静态文件，部署前必须构建：

```powershell
cd 前端
npx vite build
# 输出到 前端/dist/，包含 index.html + JS/CSS 资源
```

验证构建产物：

```powershell
Test-Path 前端\dist\index.html   # 应为 True
```

## 八、启动服务

### 后端 CLI 工具 `lasr` 说明

文档中多次出现的 `.venv\Scripts\python.exe -m lasr serve` 和 `.venv\Scripts\python.exe -m lasr certificates` 都指向同一个后端命令行工具。以下详细解释其身份与工作原理。

#### `lasr` 是什么

`lasr` 是本项目的**后端 Python 包名**，全称为 **LASR Local Voice Studio**（听间）。它同时是：

| 身份 | 说明 |
|------|------|
| Python 包 | `后端/lasr/` 目录，包含全部后端源码（FastAPI 应用、音频推理、协议、安全等模块） |
| CLI 工具 | 通过 `pyproject.toml` 的 `[project.scripts]` 注册为 `lasr = "lasr.cli:main"`，安装后可直接执行 `lasr serve` 或 `python -m lasr serve` |
| 项目简称 | 整个“本地离线实时语音识别前后端项目”的缩写 |

#### `python -m lasr` 的入口链路

```
python -m lasr
  → lasr/__main__.py          # multiprocessing.freeze_support() + 调用 main()
    → lasr/cli.py::main()     # argparse 解析子命令
      → serve / prepare-models / certificates
```

`__main__.py` 存在的原因：Windows 的 `multiprocessing` 使用 `spawn` 创建子进程时，子进程会重新导入主模块。`freeze_support()` 确保子进程导入不会递归启动 Uvicorn 服务器。

#### `lasr` 的三个子命令

| 子命令 | 功能 | 是否需要联网 | 何时使用 |
|--------|------|------------|----------|
| `serve` | 启动真实识别服务 | 否（模型需已就绪） | 开发调试或正式部署时 |
| `prepare-models` | 下载并校验模型文件 | 是（首次下载） | 首次部署或校验完整性 |
| `certificates` | 生成项目私有 CA 与服务器证书 | 否 | 正式局域网部署时 |

#### `lasr serve` 做了什么

这是核心服务命令，启动后后端将作为一个完整的局域网语音识别服务器运行。内部执行流程：

```
CLI 解析参数
  → Settings 配置对象（host/port/tls/model_dir/allowed_hosts 等）
    → settings.validate() 校验配置合法性
      → create_app(settings) 组装 FastAPI 应用
        → ProcessWorker(settings) 创建 spawn 进程管理器
          → lifespan 启动:
              → coordinator.open()
                → 创建 4 个 IPC 队列（audio/control/result/terminal）
                → spawn 子进程 lasr-cpu-worker
                  → worker_main() 加载 sherpa-onnx 模型
                  → 模型就绪后发送 loaded 事件
              → console.start() 启动终端控制台（显示配对口令）
        → SecurityMiddleware 安全中间件（Host/Origin 校验）
        → install_routes() 安装路由:
            GET  /health         健康检查
            POST /api/pair       配对（口令换会话 cookie）
            POST /api/logout     注销
            GET  /api/status     状态查询
            WS   /ws             音频流 WebSocket
            GET  /               前端静态文件（index.html）
            GET  /*              前端静态资源
      → uvicorn.run() 启动 ASGI 服务器
```

关键设计特征：

- **单进程推理**：Uvicorn 主进程处理 HTTP/WebSocket，通过 IPC 队列与独立的 `lasr-cpu-worker` 子进程通信，模型加载和推理完全在子进程中执行
- **单轮次独占**：同一时刻只允许一个录音轮次使用推理器，多客户端通过协调器排队
- **安全默认**：禁用 FastAPI 自动文档（`docs_url=None`）、拒绝未授权请求、cookie 设置 `httponly` + `samesite=strict`

#### `lasr certificates` 做了什么

生成项目私有的 TLS 证书链，用于正式局域网部署（手机等外部设备需要 HTTPS 才能使用麦克风）。

```
CLI 解析参数（--san <IP/域名> --days <有效期>）
  → generate_certificates(output, names, days)
    → 生成 EC P-256 CA 私钥 + 自签名 CA 证书（有效期 366 天）
    → 生成 EC P-256 服务器私钥 + CA 签发的服务器证书（含 SAN 扩展）
    → 签名验证（确保链完整）
    → 原子写入到 .cache/backend/tls/ 目录:
        ca/ca-key.pem          CA 私钥（0600 权限）
        ca/ca-cert.pem         CA 公钥证书（分发给客户端信任）
        server/server-key.pem  服务器私钥（0600 权限）
        server/server-chain.pem 服务器证书链（服务器证书 + CA 证书）
        certificate-metadata.json  证书元数据（SAN、有效期、指纹）
```

生成后使用方式：

```powershell
cd 后端
.venv\Scripts\python.exe -m lasr serve --host 192.168.3.104 --port 8765 \
  --tls-cert .cache\backend\tls\server\server-chain.pem \
  --tls-key .cache\backend\tls\server\server-key.pem \
  --allowed-host 192.168.3.104:8765 \
  --allowed-origin https://192.168.3.104:8765
```

注意：此命令生成的 CA 不会自动安装到系统信任存储。手机需手动导入 `ca-cert.pem` 或在浏览器中接受证书警告。开发模式建议使用 `mkcert`（自动安装根 CA），此命令更适合生产部署。

### 模式 A：开发模式（HTTP 明文，仅 localhost）

适用于本机调试，不需要 TLS 证书。

**终端 1 — 后端：**

```powershell
cd 后端
.venv\Scripts\python.exe -m lasr serve --host localhost --port 8765 --dev-localhost --no-console
```

启动后终端会显示一行：

```
LASR_PAIRING_CODE=XXXXXXXXXXXXXXXX
```

**记下这个口令**，后面人工测试要用。

**终端 2 — 前端开发服务器（可选，用于热更新开发）：**

```powershell
cd 前端
npx vite
# 访问 http://localhost:5173
# Vite 已配置代理: /api → http://localhost:8765, /ws → ws://localhost:8765
# 注意：代理必须 changeOrigin: true，否则后端安全中间件因 Host/Origin 不匹配返回 403
```

> 如果已构建 dist，可以不开前端开发服务器。后端直接提供静态文件（见下方）。

### 模式 B：LAN 部署（HTTPS，需 TLS 证书）

适用于局域网内其他设备访问。

**步骤 1：生成项目私有 CA 和服务器证书**

```powershell
cd 后端
# SAN 填写实际要访问的 IP 或主机名，可重复 --san
.venv\Scripts\python.exe -m lasr certificates --san localhost --san 192.168.1.100 --days 90
# 输出到 .cache/backend/tls/
# 记录 ca_sha256 和 server_sha256 供人工核对
```

生成的文件结构：

```
.cache/backend/tls/
├── ca/
│   ├── ca-key.pem       (CA 私钥，0600 权限)
│   └── ca-cert.pem      (CA 证书，分发给客户端导入)
├── server/
│   ├── server-key.pem   (服务器私钥，0600 权限)
│   └── server-chain.pem (服务器证书链 = 服务器证书 + CA 证书)
└── certificate-metadata.json
```

**步骤 2：启动 HTTPS 服务**

```powershell
cd 后端
.venv\Scripts\python.exe -m lasr serve ^
    --host 0.0.0.0 --port 8765 ^
    --tls-cert .cache\backend\tls\server\server-chain.pem ^
    --tls-key .cache\backend\tls\server\server-key.pem ^
    --allowed-host localhost:8765 --allowed-host 192.168.1.100:8765 ^
    --allowed-origin https://localhost:8765 --allowed-origin https://192.168.1.100:8765 ^
    --cpu-threads 2
```

> `--static-dir` 默认为 `前端/dist`，如果已构建则无需指定。

## 九、人工测试验证流程

以下步骤在浏览器中执行。假设开发模式 `http://localhost:8765`。

### 7.1 存活检查

```
浏览器访问: http://localhost:8765/health
预期: {"status":"ok"}
```

### 7.2 配对流程

1. 打开 `http://localhost:8765/`
2. 页面显示配对表单（"听间 · 本地语音工作台"）
3. 在后端终端记下 `LASR_PAIRING_CODE=XXXXXXXXXXXXXXXX`
4. 在页面输入框填入该口令，提交
5. 预期：表单消失，进入工作台主界面（配对成功，cookie 已设置）

**错误场景验证：**

- 输入错误口令 → 应显示错误提示，5次后锁定5分钟
- 输入带空格的口令 → 应自动去首尾空白并转大写

### 7.3 状态检查

配对成功后，工作台应显示：

- 状态栏显示连接状态
- 可通过浏览器开发者工具 Network 面板查看 `/api/status` 响应：

```json
{
  "protocol_version": "1.0",
  "config_id": "zipformer-...",
  "model_ready": true,
  "busy": false,
  "max_active": 1,
  "max_utterance_ms": 60000,
  "connection_available": true
}
```

### 7.4 录音识别（核心流程）

**桌面端 — 鼠标长按：**

1. 按住工作台上的录音按钮（左键）
2. 对着麦克风说一段话（中文或英文，如"今天天气不错"）
3. 松开按钮
4. 预期：
   - 按住期间出现实时草稿气泡，文字逐渐更新
   - 松开后气泡变为终稿，不再变化
   - 状态栏显示"已完成 · 本地流式识别"

**桌面端 — 空格长按：**

1. 焦点在工作台区域时，按住空格键
2. 说话 → 松开空格
3. 预期同上

**异常场景：**

- 按住后立即松开（<1秒）→ 应显示"准备期间已松手"
- 说话后等 60 秒不松手 → 应自动停止并生成终稿
- 录音过程中切换浏览器标签 → 应取消本轮

### 7.5 结果管理

- 终稿气泡右下角有复制按钮 → 点击复制到剪贴板
- 失败的气泡不显示复制按钮
- 取消的气泡显示取消原因
- 超过 50 张卡片后旧卡片被移除

### 7.6 取消与错误恢复

- 录音中按 `Escape` → 本轮取消，显示"已取消"
- 录音中切到后台再回来 → 本轮取消
- 拔掉麦克风 → 本轮中断，显示设备丢失提示

### 7.7 注销

- 点击注销按钮
- 预期：回到配对表单，cookie 已清除
- 重新访问 `/api/status` → 返回 401

### 7.8 控制台命令

在后端运行服务的终端中：

- 输入 `pair` → 重新生成配对口令
- 输入 `revoke-all` → 撤销所有会话

### 7.9 移动端验证（需 LAN HTTPS 模式）

1. 在手机浏览器导入 CA 证书（`ca-cert.pem`）为受信任根证书
2. 访问 `https://192.168.1.100:8765/`
3. 配对 → 长按录音 → 松手成文
4. 验证触摸长按、单指互斥、安全区布局

## 十、完整执行顺序速查

```
1. 安装依赖
   后端: python tools\prepare_env.py
   前端: cd 前端 && npm install

2. 模型校验
   cd 后端 && .venv\Scripts\python.exe -m lasr prepare-models --verify-only

3. 自动化测试
   后端: .venv\Scripts\python.exe -m pytest tests/ -v
   前端: npx vitest run

4. 类型检查与 Lint
   后端: .venv\Scripts\python.exe -m ruff check lasr/ tests/
   前端: npx vue-tsc --noEmit

5. 构建前端
   cd 前端 && npx vite build

6. 启动服务（开发模式）
   cd 后端 && .venv\Scripts\python.exe -m lasr serve --dev-localhost --no-console

7. 人工验证
   浏览器 → http://localhost:8765/ → 配对 → 录音 → 验证终稿
```

## 十一、已知限制（明确列出，不作通过保证）

| 项目 | 状态 | 说明 |
|------|------|------|
| 普通低配 CPU 的 CER/WER | 未测 | 需实际设备语料 |
| P95 延迟基线 | 未测 | 需计时工具与标准语料 |
| 物理麦克风兼容性 | 未测 | 需真实设备验证 |
| Android/iOS 证书与后台行为 | 未测 | 需真机 + CA 导入 |
| 8 小时长稳 | 未测 | 需持续运行与监控 |
| Playwright 端到端 | 代码已引入依赖 | 需实际运行 `npx playwright test` |

以上项目 spec 明确标注"未实际执行则明确列出，不作通过保证"。
